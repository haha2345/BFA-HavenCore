# 方案 — 腐蚀剩余修复（实施总排期）

对照：**8.3.7.35662 最终版（含热修）**。底本 `BFA-HavenCore/`，家 `src/server/scripts/Spells/spell_corruption.cpp`。
每条效果的链路/数字见 [腐蚀特效清单.md](腐蚀特效清单.md)，系统铁律见 [设计-腐蚀-Blizzlike.md](设计-腐蚀-Blizzlike.md)。本文只回答三件事：**剩下什么、按什么顺序修、怎么测**（含多人参与的效果）。

---

## 现状快照（2026-08-29）

| 已过 | 内容 |
|------|------|
| A1 无尽之星 | 第 1 层通过（`.labstars`） |
| A2 暮光毁灭 | 光柱主路径通过；P1 十目标/半伤 **进游戏验收通过**（2026-08-29） |
| A3 虚空回响 | 主路径通过；P1 无 GCD 过滤 **进游戏验收通过**（2026-08-29） |
| A4 扭曲的附肢 | 第 1 层通过（`.labtentacle` + `.labaoe`） |
| A5 虚空仪式 | P0 **进游戏验收通过**（2026-08-29） |
| P2 被动 7 条 | **进游戏验收通过**（2026-08-29）；虹吸者回血不进战斗记录已接受 |
| P3 次级 7 条 | 第 1 层全部通过（2026-08-28） |
| P4 坏效果六档 | **进游戏验收通过**（2026-08-29）。彼岸之物视觉为署名近似（29406 + 319695），偏差已登记 |
| P5 真装管道 2b | **进游戏验收通过**（2026-08-29） |
| P6 尼奥罗萨武器写死 C | **第 1 层通过**（2026-08-29，见 方案-P6-尼奥罗萨武器.md） |

---

## 修复总清单（P0 → P6）

一次一批，每批独立验收、独立提交。批内一次一条。

| 批 | 内容 | 预计脚本量 | 挡不挡下一批 |
|----|------|-----------|--------------|
| **P0** | A5 收尾：相位检查 + ritual 多人 pack **进游戏验收通过**（2026-08-29） | 0–小改 | 不挡 P1/P2 |
| **P1** | A2 十目标/半伤 + A3 无 GCD **进游戏验收通过**（2026-08-29） | 小 | 不挡 |
| **P2** | 被动 7 条 + 7 张 stat pack **进游戏验收通过**（2026-08-29）；击穿 320249 已写脚本（DBC BP=0） | 0–小改 | 不挡 |
| **P3** | 次级触发 7 条 **第 1 层进游戏全部通过**（2026-08-28）；真装驱动随 P5 关账 | 中 | 不挡 |
| **P4** | 坏效果 B 六档 **进游戏验收通过**（2026-08-29） | 中大 | 真装 2b 的坏效果验收依赖它 |
| **P5** | 真装管道 2b **进游戏验收通过**（2026-08-29）：外壳随装备、ilvl CalcValue、多件 Dummy 求和 | 中 | 收尾 |
| **P6** | 六条武器 **第 1 层通过**（2026-08-29）。方案见 方案-P6-尼奥罗萨武器.md。 | 中 | 不挡 |

### 排序理由

- **P0 最先**：A5 是唯一「主路径过了但多人分支没实测」的签名效果。多人测试基建（双客户端流程）建好后，P4 彼岸之物可见性、P5 多件叠加都要复用。
- **P1 第二**：A2/A3 的缺口是「已上线代码和 35662 最终版之间的已知偏差」，比新效果优先——旧账不清，后面每次回归都要带着这两条已知差异跑。
- **P2 极便宜**：被动 7 条大概率零脚本，一次进游戏 `.aura` 全部扫完，把「未测」变成「已验」或「要修」，半天内消掉 7 行未知。
- **P3 在 P4 前**：次级触发和签名共用同一套 proc/光环形状（隐藏 proc → buff），风险低；坏效果 B 要写召唤 AI（眼睛、彼岸之物），形状新，放后面。
- **P3 内部顺序按脚本量升序**：急速脉搏/磨砺心灵/致命之势（proc 链 DBC 基本齐，脚本只补层数行为）→ 活力涌动（受伤触发，测法不同）→ 龟裂创伤（BP=0 填伤，抄 A4 的 CalcAmount 形状）→ 须臾洞察（「下一技能 CD −3s」，新形状）→ 不可言喻的真相（recharge rate 修改，最生疏）。
- **P4 在 P5 前或并行**：设计文档定的顺序。真装 2b 验收里「穿 75 腐蚀出星 + 出眼」需要 B 层至少前三档能跑。
- **P6 第 1 层通过**（2026-08-29）：方案见 方案-P6-尼奥罗萨武器.md。双持双手加倍仍不做。

---

## 各批实施细则

### P0 — A5 收尾（含多人）

代码已在（`spell_void_ritual_proc` / `spell_void_ritual_end_is_coming`）。P0 **进游戏验收通过**（2026-08-29）：

1. **进游戏单人验收**按 [做法-A5-虚空仪式.md](做法-A5-虚空仪式.md) 的第 1 层清单跑完（黄字开 buff、1→20 爬坡、20 秒整段掉、平砍不开、已有 buff 不刷新、面板四维上涨）。**通过。**
2. **多人分支实测**：见下文「多人参与效果的测试方案」。HavenLab ritual pack 已出「多人矩阵」小节。**通过。**
3. 加固 a：**代码完成。** `VoidRitualNearbyAllies` 在网格扫描上补了 `IsInPhase`（死亡豁免已有 `IsAlive`）。
4. 加固 b：**代码完成。** 单人 5/6 保持 proc 后再掷；热修白名单已注明这是 SimC 模型近似（无坏运气补偿）。

验收线（多人部分）：双端 8 码内两人都挂 316814 时，proc 日志全部 `increased=1`、零 `skipped=solo`；拉开 >8 码或摘掉一端光环后恢复出现 `skipped=solo`。DBC 字面 `allies >= 2`（除自己外两人）先不动，矩阵 #2 一人侧允许 skip。

### P1 — A2 / A3 热修收口

- **A2 十目标 + 半伤**：**进游戏验收通过**（2026-08-29）。`at_twilight_devastation` 按进入顺序 `_hitCount` + GUID 去重；第 6–10 个伤害 ×0.5（`SPELLVALUE_BASE_POINT0`），第 11 个起不结算，打满 10 个 `at->Remove()`。twilight pack `expect = { maxTargets=10, halfFrom=6 }`。
- **A2 验收**：`.labaoe` 拉 12 只桩排成一线，CLEU 应出现恰好 10 条光束伤害，第 6–10 条约为前 5 条一半，第 11、12 只无伤害。**通过。**
- **A3 无 GCD 过滤**：**进游戏验收通过**（2026-08-29）。`HandleEchoingVoidProc` 在 `PreventDefault` 之后检查触发技能 `StartRecoveryTime==0`（含无 `GetProcSpell` 的平砍）则直接返回。echo pack `hintFail`：无 GCD 技能叠了层 = 过滤失效。
- **A3 拉野怪**：坍缩 AoE 扫到未进战的怪要拉进战斗（设计决定 2，blizz-like，不做保护）。**通过**，写进测试记录而不是当 bug。

### P2 — 被动 7 条（先验后修）

进游戏一轮 `.aura` 扫描（一段驱动 ID 见清单总表），记录人物面板变化：

| 条 | 挂 | 看 | 预期 |
|----|-----|-----|------|
| 权宜之计 315544 | `.labexpedient` | 急速 rating/% | **第 1 层通过**（2026-08-28）：1945/+28.60% → 2061/+30.31%（×1.06），零脚本 |
| 娴熟 315529 | `.labmasterful` | 精通 rating/% | **第 1 层通过**（2026-08-28，玩家确认面板）。来源×1.06，零脚本 |
| 多才多艺 315549 | `.labversatile` | 全能 rating/% | **第 1 层通过**（2026-08-28，玩家确认面板）。来源×1.06，零脚本 |
| 暴戾 315554 | `.labsevere` | 暴击 rating/% | **第 1 层通过**（2026-08-28，玩家确认面板）。来源×1.06，零脚本 |
| 虹吸者 315590 | `.labsiphoner` | 吸血 | **第 1 层通过**（2026-08-29）：面板 +3%。回血不进战斗记录，已接受，不补 143924 |
| 击穿 315277 | `.labstrikethrough` | 暴击伤害（CLEU） | **第 1 层通过**（2026-08-28）：剑刃风暴 4211→8590（2.040）；无击穿时 4211→8422。脚本填 320249 Dummy 2 |
| 闪避者 315607 | `.labavoidant` | 闪避 = 急速的 8% | **第 1 层通过**（2026-08-29）。核心类型 198 接到 `UpdateRating`（闪避 rating = 急速 rating × Amount%）。不要写死 8/12/16 |

规则：面板变了就登记「已验零脚本」，一行都不写；没变的才开工。击穿/闪避者若要脚本，形状是 `DoEffectCalcAmount` 从驱动 Dummy 读数填 hidden aura，禁止写死 2/3/4。

P2 代码侧（2026-08-27）：7 张 `want=stat` pack + `.lab test` 七行已交付。35662 dump 确认 **320249 BP=0**，已写 `spell_strikethrough_driver/hidden`（只填暴击伤 Dummy 2/3/4；治疗走驱动 EFFECT_1 活 aura，320249 治疗保持 0）。闪避者核心 `SPELL_AURA_198` 已接到 `UpdateRating`。**进游戏验收通过**（2026-08-29）。

### P3 — 次级触发 7 条

共同形状（全部沿用 A5 的骨架）：驱动 LINKED → 隐藏 proc（RPPM/掩码**全信 DBC，不写 `spell_proc`**）→ 属性 buff。每条差异点：

| 条 | 脚本要点 | 验收要点 |
|----|----------|----------|
| 急速脉搏 318266 | **第 1 层通过**（2026-08-28）。buff 4 秒不叠层，rating 读驱动 Dummy | `.labpulse` 挂 318220；`PULSE_PROC rating=546`；318227 末次触发后约 4s 掉、刷新不叠层；面板 hasteRating +557。真装驱动 P5 |
| 磨砺心灵 318269 | **第 1 层通过**（2026-08-28）。`MIND_PROC rating=392`；快照 +392；白字可 proc；再 proc 刷新约 10s、不叠层 | `.labmind` 挂 318214 |
| 致命之势 318268 | **第 1 层通过**（2026-08-28）。`DoCheckProc` 仅暴击；CalcAmount 只填每层 Dummy（引擎乘层）；默认刷新 | `.labmomentum`：1→5 层 `rating=31/62/93/124/155`；满层后再 proc 仍 5。**未测**停手 30s 掉光 |
| 活力涌动 318270 | **第 1 层通过**（2026-08-28）。真怪挨打挂 318211，约 20s，全能 +350。掉光约 11.5s 后再挂，仍约 20s、不叠层。无 `VITAL_PROC`。增益还在时刷新未测 | `.labvitality` 挂 318212；不要打木桩 |
| 龟裂创伤 318272 | **第 1 层通过**（2026-08-28）。活怪暗影跳伤约 7s/7 跳；1421=13%×攻强，暴击两倍。团本桩免疫。本份无 `WOUND_PROC`（上一份已有）。刷新未单独测 | `.labwound` 挂 318179；打活怪 |
| 须臾洞察 318239→315574 | **第 1 层通过**（2026-08-28）。315573 会挂会叠层；有 CD 技能减层并 −3s（转盘确认）；无 CD 技能不减层。无 `CD_TRIM`（须 GM 模式）。自然 15s 掉光未测 | `.labglimpse` 挂 315574；不要开冷却作弊 |
| 不可言喻的真相 318303 | **第 1 层通过**（2026-08-28）。316801 约 10s 自然掉；`RECHARGE pct=30 mult=76`。已有 buff 再 proc、二段 50 未测 | `.labtruth` 挂 316799 |

每条一个提交 + 一份 `spell_script_names` SQL。HavenLab 各加一条 `.lab*`（卸载其他腐蚀 proc、挂本条驱动），保持「一次只测一条」。

### P4 — 坏效果 B 六档

进入 C 层（阈值光环挂上之后的行为）。**`UpdateCorruption()` 循环一行不改**；只给 315175 / 315169 / 315184 / 315857 / 315179 / 337612 写行为脚本。这一层脚本**允许读有效腐蚀值**（坏效果自身的缩放），仍禁止读阈值（阈值归 DB2）。

开工前置：2a 管道验证——按 [方案-A-腐蚀-测试.md](方案-A-腐蚀-测试.md) 做测试戒指（`ITEM_MOD_CORRUPTION` 25/80 两档 hotfix SQL，标注测试用），确认阈值套/摘光环已通，再写行为。

| 档 | 效果 | 形状 | 关键细节（全文见清单 B 节） |
|----|------|------|------------------------------|
| 1+ | 蔓生触须 315175 | **进游戏验收通过**（2026-08-29）。OnProc → 315176；幅度 = 有效腐蚀+10 上限 99 | `.labgrasping` 挂 315175；挨打出 315176 |
| 20+ | 腐蚀之眼 315169 | **进游戏验收通过**（2026-08-29）。模板 22815 + `at_eye_of_corruption` 走近跳 315161 | `.labeye` 挂 315169；走进眼睛等 2s 看 `EYE_PULSE` / 315161 |
| 40+ | 宏伟妄想 315184 | **进游戏验收通过**（2026-08-29）。碰撞伤 315197、8 秒超时 despawn、29406 罩体 + 319695 连线。视觉为署名近似，偏差已登记 | `.labdelusion` 挂 315184；活怪还手；`DELUSION_PROC` / `DELUSION_HIT` |
| 60+ | 层叠灾难 315857 | **进游戏验收通过**（2026-08-29）。`DELUSION_HIT` 处调用触须/眼触发入口，不套 315175/315169 | `.labcascade` 只挂 315857；另 `.aura 315184` 才能出怪 |
| 80+ | 不可避免的厄运 315179 | **进游戏验收通过**（2026-08-29）。`spell_inevitable_doom` 幅度 = Dummy×(有效腐蚀−50) | `.labdoom` 挂 315179 |
| 200 | 末路恶果 337612 | **进游戏验收通过**（2026-08-29）。战斗中每秒 337816（25% 最大生命） | `.labconsequences` 挂 337612；关 god |

顺序即依赖序：60 档依赖 40 档的彼岸之物和 1/20 档的触发技能，必须最后三条按序做。召唤物统一守则见「稳健性」节。

### P5 — 真装管道 2b

按设计文档四步：**进游戏验收通过**（2026-08-29）。

- 外壳 → 驱动：324889 AfterCast 改为挂对应段位驱动；`spell_corruption_rank_driver` 在任一驱动还在时保持 hidden proc，最后一件脱掉才摘。
- ilvl：求值改 `CalcValue` + 施法物品 `GetItemLevel`，不动 proc/落点/延迟。
- 多件同效果：`SumCorruptionRankDummy` 对已挂驱动 Dummy **求和**，不再取最高段。双驱动 `.aura` 两段叠挂验数字。
- 腐蚀值联动：仍走 `UpdateCorruption()`，本批不改循环。

系统级验收即设计文档的五条勾选框（穿脱、好坏并存、抵抗掉档、AT 判定同源、拉怪写进测试步骤）。**通过。**

偏差（不挡代码完成）：星/暮光/回响/触须的 `CalcValue` 打在百分比 Dummy 上，aura 285 / RandPropPoints 是否进伤害待验；龟裂 318272 装等系数未接，仍读 318187 Dummy 13%。须臾减 CD 只读 315573/315574 Dummy 3s，不读 318239。

---

## 多人参与效果的测试方案（重点：虚空仪式）

### 为什么必须双端

`VoidRitualNearbyAllies` 只数 `Player`，HavenLab 木桩、召唤物、镜像都不算。**没有第二个真实玩家在线，`increased` 分支永远走不到**——这是当前 A5 唯一没有证据的分支。同样的基建 P4 彼岸之物可见性、P5 多件/团队场景都要用。

### 双端测试环境（一次搭好，复用）

1. worldserver 控制台 `bnetaccount create test2@haven <密码>`，`account set gmlevel` 给到能用 `.aura` 的等级（只给 2 号测试机用，测完降回）。
2. 同一台机器跑两个 35662 客户端实例（复制一份 `client/` 的 WTF 或用 `-config` 分开配置，避免互踩缓存；同机双开对 3307 本地端没有限制）。
3. 两个角色进同一主城，都 `.character level 120`。
4. 主测角色 `.labritual`；2 号角色自己 `.aura 316814`（或主角色选中 2 号 `.aura`——两种都试，登记哪种在本核心可用）。
5. 距离用 `.distance`（GM 命令）标定 8 码内/外两个站位点。

### 虚空仪式测试矩阵

判定证据 = GM 日志 `RITUAL_PROC allies=N increased=X` / `skipped=solo`。每格至少攒 20 次 proc 事件再下结论。

| # | 场景 | 预期日志 |
|---|------|----------|
| 1 | 单人打桩（基线，已测过） | `skipped=solo` 占比 ≈ 1/6（样本 ≥60：二项检验，掉在 8%–28% 之外才算失败） |
| 2 | 2 号在 8 码内、挂 316814 | `allies=1`。**注意：DBC 文案是「至少 2 名」，一人是否够要在这里定案**（见下「一个待定案」） |
| 3 | 2、3 号都在 8 码内挂 316814（三端或 2 号 + 第三账号） | `allies=2 increased=1`，零 skip |
| 4 | 2 号在 8 码内但**没挂** 316814 | `allies=0`，走 solo 分支 |
| 5 | 2 号挂着但站 10 码外 | `allies=0` |
| 6 | 2 号挂着、8 码内、**死亡** | `allies=0` |
| 7 | 2 号为敌对阵营（决斗前置或对立阵营号） | `allies=0` |
| 8 | 主角色已有 316823，任何场景再 proc | `skipped=refresh`，时长层数不动 |
| 9 | 2 号在爬坡中途下线/跨相位 | 主角色已开的 316823 不受影响（proc 几率才看附近，buff 本身不看） |

**一个待定案（进游戏前先定，写进做法文档再测）**：DBC Dummy「2 人」指「附近至少 2 名**也有**虚空仪式的友方」。当前代码 `allies >= need(2)` 意味着需要**除自己外 2 人**（三人小队才满足）。SimC 模型只有「有人/没人」二值。建议按 DBC 字面（≥2 名友方）实现不动，把场景 2（1 人）预期定为 `skipped` 可能出现；若进游戏观感与正式服记录矛盾，只改 `VoidRitualAllyNeed` 的比较处，一行。

### 其余多人相关效果的测法（做到哪条测哪条）

| 效果 | 多人属性 | 测法 |
|------|----------|------|
| 宏伟妄想（P4） | 彼岸之物只对「自己+同效果者」可见 | 双端：2 号无腐蚀时应看不见（若可见性做不了，登记偏差）；2 号 ≥40 腐蚀时可见 |
| 层叠灾难（P4） | 被追击者与旁观者各自结算 | 双端各 ≥60 腐蚀，只有被命中者立刻吃触须+眼 |
| 暮光毁灭光柱 / 虚空回响坍缩 / 触须 AoE | 不得误伤友方玩家 | 2 号站光柱路径/坍缩半径内：无 PvP 状态不掉血；决斗中按 PvP 系数（暮光 0.5 热修） |
| 虚空回响 | 多人各自叠层互不串 | 双端都 `.labecho`，各自 317020 层数独立 |
| 真装多件（P5） | 团队环境光环同步 | 双端互相 inspect / 观察对方 buff 图标与自己面板一致 |

### 多人回归纪律

- 每次动 proc 入口（`CastVoidRitual` 一类唯一入口函数）后，矩阵 1/3/4/8 四格必须重跑；其余格季度性抽查。
- 日志行保持 GM-gated（`.gm on` 才打），格式不改（`allies= increased= skipped=`），矩阵的判定脚本化依赖这些字段。
- 双端流程写进 [方案-HavenLab-测试台.md](方案-HavenLab-测试台.md) 的准备节，避免每次现查命令。

---

## 验收插件配套（HavenLab，与效果代码同批交付）

现状：插件 2.0 数据驱动引擎已上线（源 `addons/HavenLab/`，镜像 `client/World of Warcraft/_retail_/Interface/AddOns/HavenLab/`）。测试台文档 [方案-HavenLab-测试台.md](方案-HavenLab-测试台.md)：**批次一已完成**；批次二第 6–9 条与批次三总览随 P0–P5 **进游戏验收通过**（2026-08-29）。

铁律沿测试台文档：**新增效果 = 服务端 `.lab test` 表加一行 + `HavenLab_Packs.lua` 加一张 pack，不改引擎**；引擎能力不够先补引擎再加 pack。插件只观测和判定，期望值全部来自 pack 数据并标注来源（各做法文档）。服务端→插件只走 `[HavenLab] <TYPE> k=v` 系统消息一条信道，GM-gated。**改完 `addons/HavenLab/` 必须同步镜像到客户端 AddOns 目录**，否则验收看到的是旧版。

| 批 | 插件交付（与该批效果代码同批完成） |
|----|--------------------------------------|
| P0 | ritual pack 补多人字段：解析 `RITUAL_PROC allies= / increased= / skipped=`，报告输出「多人矩阵」小节。**进游戏验收通过**（2026-08-29）。 |
| P1 | twilight pack `expect = { maxTargets=10, halfFrom=6 }` **通过**（2026-08-29）；echo pack 无 GCD `hintFail` **通过**（2026-08-29） |
| P2 | 权宜/娴熟/多才/暴戾/击穿/闪避者/虹吸者第 1 层 **通过**（2026-08-29）。虹吸者回血不进战斗记录已接受。 |
| P3 | 7 张 pack **已交付**。急速脉搏、磨砺心灵、致命之势、活力涌动、龟裂创伤、须臾洞察、不可言喻的真相第 1 层 **全部通过**（2026-08-28）。 |
| P4 | 坏效果六档 pack **进游戏验收通过**（2026-08-29）。`THRESHOLDS` 已填 DB2 Aura：315175/315169/315184/315857/315179/337612。 |
| P5 | 穿脱 + 总览 + Dummy 求和 **进游戏验收通过**（2026-08-29）。 |

插件文件全是 Lua，无编译步骤；每批交付时 pack 数据的期望值必须能溯源到清单或做法文档，禁止在插件里发明服务端真相。

---

## 系统稳健性守则（新增条目并入总纲）

从 A1–A5 已提炼的不重复（伤害只走 SetHitDamage/CalcAmount、GUID 跨帧、PreventDefault 唯一入口、不写 `spell_proc`、ID 全走 enum、GM 才打日志）。本方案新增：

1. **求值函数单点化**：每条效果的「段位→数字」只有一个函数（如 `VoidRitualRatingPerStack`），P5 多件叠加只改这些函数，不碰行为代码。
2. **fallback 必须带一次性 ERROR 日志**：所有 `GetEffect` 失败回退常量的地方照 `InfiniteStarsVulnPct` 的样子打一次 `TC_LOG_ERROR`，静默回退禁止——DB2 缺行要在启动后第一次触发时暴露。
3. **召唤物四件套**（触须已达标，眼/彼岸之物照抄）：owner 光环没了立即 despawn；faction/level 跟 owner；`REACT_PASSIVE` + 显式目标选择；命中/引导以 owner 为 originalCaster 保证 CLEU 归属和暴击表。
4. **坏效果读值边界**：C 层脚本可读**有效腐蚀值**做自身缩放（触须幅度、眼范围），但阈值判断永远不出现在脚本里；grep 关卡：`spell_corruption.cpp` 里不许出现 `>= 20`/`>= 40` 一类腐蚀阈值比较。
5. **热修白名单持续登记**：P1 的十目标计数、P4 的触须 99% 上限、P0 的 5/6 模型注记，全部进设计文档的热修表，来源列蓝贴/Wowhead 热修日期。
6. **性能红线**：附近玩家扫描（仪式 8 码）只在 proc 时刻做一次网格遍历，禁止挂周期轮询；AT 命中集合用 AT 实例成员，生命周期 ≤ AT 本体。
7. **每批合并前回归**：跑一遍已过效果的 `.lab*` 快检（星/光柱/回响/触须/仪式各 1 分钟），确认新脚本没有把共享工具函数改坏。P3 起效果多了以后，快检清单维护在 HavenLab 文档里。

---

## 提交与完成定义

- 一条效果一个提交，格式沿用 `Scripts/Spells: Implement <Effect> corruption effect`；SQL 一律 `sql/updates/world/2026_MM_DD_NN_world_corruption_<effect>.sql` 且只插 `spell_script_names`。
- 每条效果合并时同步更新：README 进度表一行 + 对应做法/清单的 Haven 现状列。未进游戏必须写明。
- 插件改动与对应效果同一个提交（服务端 `.lab test` 行、pack、镜像同步一起进），HavenLab 引擎改动单独提交。
- **本方案完成定义**：P0–P6 **进游戏验收通过**（2026-08-29）= 清单 A + B + C 武器写死在第 1 层可玩，且每条效果都有对应 HavenLab pack、批次二/三插件能力交付完毕。五人 / 词缀另开方案。
