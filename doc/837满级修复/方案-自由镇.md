# 方案 — 自由镇（普通 / 英雄 / 史诗）

对照版本：**8.3.7.35662**。副本地图 **1754**。冒险指南实例 **1001**。区域 **9164**。  
底本：`BFA-HavenCore/`。家：`src/server/scripts/KulTiras/FreeHold/`（填现有文件，不平行再造）。  
调研定稿：[调研-自由镇.md](调研-自由镇.md)（R1+R2+R3）。代码标准：`.grok/skills/837-blizzlike-fix/references/philosophy.md` 的「五人本」+「总原则」+「数据与SQL」+「验收」。  
范围：普通（难度 1）/ 英雄（2）/ 史诗（23）。**大秘境（难度 8、词缀、钥石时限、觉醒尖塔）本阶段不做。** 进游戏由用户做；本文只给待验清单。本文件写给没读过对话历史的实施员。

细节数字与逐步改法不在本文件展开，分别见：

| 细节文档 | 覆盖 |
|----------|------|
| [细节-F1-难度与血伤.md](细节-F1-难度与血伤.md) | 硬乘叠乘、三套数字、257821 叠层、白字表 |
| [细节-F2-友好船长周事件.md](细节-F2-友好船长周事件.md) | 周轮换、Murphy / Otis / 高亮指节手 / 喝酒、入伙后帮派小怪 |
| [细节-F3-四王机制差距.md](细节-F3-四王机制差距.md) | 克拉格 256056、俯冲/扔鱼饵、议会 DONE、抛鲨、掷弹兵、酒保丢酒 |
| [细节-F4-掉落与Encounter.md](细节-F4-掉落与Encounter.md) | 单表、4776、节日帽/钥石、288636、手册/战斗遭遇两套编号 |
| [细节-F5-进本进度与门.md](细节-F5-进本进度与门.md) | SetHeaders、GetData、CheckRequiredBosses 调用点、本阶段不做顺序门 |

---

## 实施员必读（现状一句话）

Haven 里四王 C++ 已挂上、小怪走 SAI、instance 骨架极薄。玩家能从泰拉加德海峡进 1754，但不能按 8.3 规则打完四王并正确结算：史诗血量会 ×2 再 ×1.33；议会打死两名敌对永远 DONE 不了、箱子无 loot；克拉格从不施放 256056、战斗座骑 126841 无 ScriptName；友好船长被做成「打死两只普通 129547 → 只有劳尔」；俯冲与扔鱼饵用 `ScriptedAI::IsHeroic()`，史诗 23 不排。

---

## 全局铁律

后文每个 Task **默认包含**本节，不再逐条复述。违反任一节即该 Task 未完成。

1. **不改 C++ 核心通用管道。** 不改 `InstanceScript.cpp` 团本血量名单、不改 `Unit.cpp` 白字乘区、不改 `Map.cpp` 的 `IsHeroic` / `IsMythic` / `GetDifficultyLootItemContext`、不改 opcode / UpdateFields。一条技能、一个王、一个掉落不改 `src/server/game/`。
2. **数据能表达的不写 C++。** 掉落、ScriptName、手册绑定、刷怪、`vehicle_template_accessory`、`creature_text`、`game_event` 走 `sql/updates/world/`。小怪战斗以 SAI 为主。`AddSC_freehold()` 可以继续空着。
3. **填现有文件。** 家就是 `FreeHold/` 下七个文件 + `kul_tiras_script_loader.cpp`（已调用，不要再造 loader）。禁止新建平行 `instance_freehold2.cpp`。禁止从 AshamaneBFA 抄空 `AddSC`。
4. **技能 ID 只来自 35662。** 禁止 413131 / 413136 等巨龙时代号段。禁止现役 Wowhead 伤害系数。插件 CD 是观察窗口，禁止写进 `EventMap` 当服务器绝对值。
5. **SQL 只追加 `BFA-HavenCore/sql/updates/world/`，文件名 `YYYY_MM_DD_NN_world_freehold_<topic>.sql`。** 不改 `Databases/` dump。一个文件一件事。能 `UPDATE` 不 `DELETE+INSERT` 整表。文件头 `-- 837 blizzlike:` 一句话原因。
6. **英雄+（俯冲 272046、扔鱼饵 272534）判定必须覆盖难度 2 与 23。** 使用 `freehold.h` 里本方案新增的 `IsFreeholdHeroicPlus`（`GetDifficultyID() == DIFFICULTY_HEROIC || == DIFFICULTY_MYTHIC`，即 spawnMode **2|23**）。禁止 `ScriptedAI::IsHeroic()` 冒充英雄+（它只认 2，史诗不排）。禁止单独 `IsMythic()`（它只认 23/8，英雄会漏）。钥石 8 本阶段不测、不为此拆 `spawnDifficulties`。
7. **禁止把下列三套数字当成可任选的 8.3 DBC：** 赛季说明英雄血伤 +25% / 非钥石史诗 +45%；`instance_difficulty_multiplier` 1.29 / 1.612；脚本 ×2 / ×1.33。详见 F1。
8. **`instance_difficulty_multiplier` 不是五人全伤倍率。** 血量通道只给五团本；伤害通道只乘白字近战 `DIRECT_DAMAGE`。禁止把该表扩到自由镇全技能伤害，禁止把它的 1.29/1.612 再乘进 `SetBaseHealth`。
9. **掉落不拆三张 `creature_loot_template`。** 装等走 `Map::GetDifficultyLootItemContext()` → `ItemBonusTree`。本图普通 MapDifficulty ItemContext **17**，会套加成列表 **4776**，不要删 4776。克拉格武器 159633 底装等 **300**，禁止改物品 ID 去凑 400/415/430。
10. **友好船长：周轮换朗姆酒区事件。** 禁止 `countRaoul >= 2` 当 8.3 规则。禁止 10.1 打完库拉格自动结盟。禁止把 MDT `week % 3` 抄到普通/英雄当选帮公式。禁止门口 gossip 三选一。
11. **议会 DONE：打死两名敌对即结束**（`captainsDeathCount >= 2`），不是 `> 2`。正式容器 GO **288636**。
12. **克拉格必须施放 256056**（Spawn Parrot，效果 28 召唤 **126841**）。把 `npc_sharkbait` 绑到 **126841**，不是 129743。129732 不是战斗座骑，本图不要当战斗体补刷。129600 本内已有 9 只，不要补刷。
13. **`CheckRequiredBosses`：本阶段不做顺序门。** 保持基类恒 `true`。禁止只改 instance 按 0–3 写门却不改克拉格/议会的 `GetEntry()` 调用点。软门（克拉格 DONE 才拆议会 `NOT_SELECTABLE`）保持原样。
14. **Encounter 两套编号不要混。** 战斗遭遇 / `ENCOUNTER_START` / `instance_encounters` / LittleWigs `engageId` = **2093–2096**。手册 / `NewBoss` 第三参 / ATT `e()` = **2102 / 2093 / 2094 / 2095**。修 `creature_template_journal` 尤朵拉错绑。不要改已经正确的 `instance_encounters (2094, 126848)`。不要用 MDT 把克拉格写成 2095。
15. **同一旗帜之下 257821：生命 +100%、伤害 +100%。** 会叠在难度基础生命上。禁止改成零售现页伤害 +25%。
16. **不改 C++ 核心、不格式化整文件、不发明字段含义。** `enum Spells` 禁止在施法处写裸数字（本副本已有各文件 enum，新增技能进对应 enum）。
17. **编译（每个改了 C++ 的 Task 完成后执行一次）：** 工作目录 `H:\wow837_projects\BFA-HavenCore`，命令 `cmake --build build --config RelWithDebInfo --target worldserver`。纯 SQL Task 不编译，但必须能在 world 库执行且不改 dump。
18. **验收不建 HavenLab 引擎，不为五人本写 `.lab` pack。** 进游戏手段：GM 命令、战斗日志（CLEU）、LittleWigs v8.3.x / DBM-Party-BfA 8.3。本方案所有「验收」都是**待用户进游戏**；实施员标「代码完成，待进游戏验收」，禁止写「已完成通关」。
19. **词缀、钥石物品系统、GO 246779、现役零售重做、ChallengeModeMgr 对 1754 的钥石装等：明确不做。** 刷怪 `spawnDifficulties` 含 8 时不要去拆 8。

---

## 分类 A–K 对照（缺项即方案未完成）

下表对照调研十一类：每类要么有 Task，要么写明「明确不做」。核心用途是实施前核对有没有漏类。

| 分类 | 名称 | 本方案 |
|------|------|--------|
| A | 进本与进度 | **Task 2**（SetHeaders、GetBossState、友好进度**内存-only**）。顺序门、墓园、坐骑、普通准入 110、LFG 模板、9000000 搬家：**明确不做**（见 Task 11 与 F5）。 |
| B | 难度管道 | **Task 1**。 |
| C | 一王克拉格 | **Task 5**。门口过场体 129732 补刷、FAIL 注释解除、俯冲轨迹 SPELL_EFFECT_254：**明确不做**（Task 11）。 |
| D | 船长议会 / 友好船长 | **Task 3**（周事件）+ **Task 4**（DONE / 257821）+ **Task 10**（酒保）。 |
| E | 战利品环 | **Task 6**。入场火焰 Todo、观众砸食物、猪点击次数改 3：**明确不做**（保持现网 5 次 / Greasy；次数进游戏验收）。 |
| F | 哈兰 | **Task 7**。413xxx、成就「斯威提的甜美战利品」必做化：**明确不做**。 |
| G | 小怪 | **Task 11**：AddSC 保持空；点名/陷阱不写 BossAI；129600 不补刷。路径/站桩：**明确不做**（进游戏验收）。 |
| H | 物件与路 | 议会箱走脚本召唤 + Task 8 loot。无 `LoadDoorData`：**明确不做**。246779：**明确不做**。 |
| I | 掉落 | **Task 8**。 |
| J | 注册接线 | **Task 5**（126841）+ **Task 9**（手册）。loader 已接，不重做。 |
| K | 明确不做 | **Task 11** 全集。 |

---

## Task 1 — 删除英雄 ×2 / 史诗 ×1.33 硬乘

**目的：** 去掉 Haven 在史诗 23 上叠成 ×2.66 的血量硬乘。删完之后三难度生物生命都走模板 `HealthModifier`（克拉格 35、三船长 22、托萨克 36、哈兰 50），不再叠脚本乘区。数字与禁止项见 [细节-F1-难度与血伤.md](细节-F1-难度与血伤.md)。

**要改的文件与函数：**

- `src/server/scripts/KulTiras/FreeHold/instance_freehold.cpp` → `instance_free_hold::OnCreatureCreate`：删除第 52–55 行两条独立 `if`（`instance->IsHeroic()` → `SetBaseHealth(* 2.f)`；`instance->IsMythic()` → `SetBaseHealth(* 1.33f)`）。`switch (creature->GetEntry())` 记 GUID 的部分原样保留。

**枚举 / ID：** 地图 1754。难度 `DIFFICULTY_NORMAL=1`、`DIFFICULTY_HEROIC=2`、`DIFFICULTY_MYTHIC=23`。难度 23 的 Difficulty.db2 Flags=**0x85**（含英雄位），所以 `Map::IsHeroic()` 在史诗上为真——这正是叠乘原因，靠删 `if` 解决，不要去改 `Map.cpp`。

**禁止事项：** 禁止改写成 ×1.25 / ×1.45。禁止读取或写入 `instance_difficulty_multiplier` 的 1.29 / 1.612 作为血量。禁止把 1754 加进 `InstanceScript.cpp` 团本血量名单。禁止改 `creature_template.HealthModifier` 去「补偿」删乘。禁止改 `creature_template_scaling` 去发明 ContentTuning 500/501/502（克拉格该行 ContentTuningID 已是 0，本阶段不接 ExpectedStatMod）。禁止动 dump。

**SQL：** 无。

**编译：** `cmake --build build --config RelWithDebInfo --target worldserver`（工作目录 `BFA-HavenCore`）。

**完成定义：** 该函数不再调用 `SetBaseHealth`；三难度同一模板生物的基础生命公式相同（模板 `HealthModifier`）；世界库无本 Task 相关脏 UPDATE。

**验收（待用户进游戏）：** 同一只小怪或克拉格，`.npc info` / 面板最大生命：英雄不得约为普通的 2 倍；史诗不得约为普通的 2.66 倍。再打一发白字与一发法术直伤，登记是否仍受表 1.29/1.612 影响（白字可能仍乘表，法术直伤不应乘该表）。三人全敌对时 257821 另测（Task 4），不要和本 Task 的基础生命混成一次结论。

---

## Task 2 — Instance 进度接线（本阶段不做顺序门）

**目的：** 五人本最低集合里的 Headers / BossState 读对；议会完成后不要再跑「没打完」分支。顺序门本阶段明确不做。入伙进度**不是落盘**，见 [细节-F5-进本进度与门.md](细节-F5-进本进度与门.md)。

**要改的文件与函数：**

- `src/server/scripts/KulTiras/FreeHold/freehold.h`：`#define DataHeader "FH"`（风暴神殿等同目录先例；地渊孢林用 `"TU"`）。在 `FreeholdData` 于 `DataMaxEncounters` **之后**增加 `DataFriendlyCaptain`（存友好船长 entry：0=无，126845/126847/126848）、`DataCrewEventDone`（0/1）。`SetBossNumber` 仍只用 `DataMaxEncounters`（4 场）。扩展 `FreeholdAction`：`ActionSelectCaptainRaoul` 之外增加 `ActionSelectCaptainJolly`、`ActionSelectCaptainEudora`。增加内联 `IsFreeholdHeroicPlus(Map const*)`（只认难度 2 与 23）。
- `src/server/scripts/KulTiras/FreeHold/instance_freehold.cpp`：
  - `Initialize`：`SetHeaders(DataHeader);` 紧挨已有 `SetBossNumber`。
  - `OnPlayerEnter`：把 `GetData(DataCounciloCaptains)` 改成 `GetBossState(DataCounciloCaptains)`。议会已 `DONE` 则不要再 `DoAction` 选船长。议会未 DONE 且 `GetData(DataCrewEventDone)` 非 0，则按 `GetData(DataFriendlyCaptain)` 对对应船长 `DoAction`（**同一副本会话内**掉线重进保持入伙，不传送。实例卸载或世界服务器重启后入伙丢失，见 F5）。
  - `OnUnitDeath`：**整段删除** `countRaoul++` / `NpcBlacktoothKnuckleduster` / `>= 2` → 劳尔。成员 `countRaoul` 删除。
  - **不要** override `CheckRequiredBosses`。基类 `InstanceScript.h:386` 恒 `return true` 保持。
  - **不要** `LoadDoorData`。
- **不要改** `boss_skycapn_kragg.cpp::EnterCombat` 的 `CheckRequiredBosses(me->GetEntry())`（传入 126832）。
- **不要改** `boss_council_o_captains.cpp::EnterCombat` 的 `CheckRequiredBosses(me->GetEntry())`（传入 126845/126847/126848）。
- **不要改** 哈兰 / 托萨克走 `_EnterCombat` 传入 `_bossId` 0–3 的路径。

**枚举 / ID：** `DataSkycapKragg=0`、`DataCounciloCaptains=1`、`DataRingOfBooty=2`、`DataHarlanSweete=3`、`DataMaxEncounters=4`。战斗遭遇 2093–2096。入口传送员 9000000 不改 BossState，保持（不是假进度）。

**禁止事项：** 禁止只按 bossId 0–3 写顺序门。禁止「一王 DONE 传送全员」。禁止把 9000000 当击杀进度。禁止本 Task 顺手改血量硬乘（那是 Task 1）。禁止把 `DataFriendlyCaptain` 算进 `SetBossNumber`。禁止把 `SetData` 写成「落盘」。禁止本阶段 override `WriteSaveDataMore` / `ReadSaveDataMore` 假装入伙已进 instance 存档。

**SQL：** 无（Headers 是 C++ 前缀，空头也能落盘 4 个 BossState；补 Headers 是最低集合格式，不是入伙存档开关）。

**编译：** 同全局铁律第 17 条。

**完成定义：** `SetHeaders("FH")` 已调用；全文件搜不到 `countRaoul`；`OnPlayerEnter` / 任何进度判断读议会时用 `GetBossState` 而不是从未 `SetData` 过的 `GetData(DataCounciloCaptains)`；`CheckRequiredBosses` 无自由镇 override；四王 `EnterCombat` 调用点未改参数。入伙只经 `SetData`/`GetData`，没有把入伙写进 `GetSaveData`。

**验收（待用户进游戏）：** `.instance` / 重进副本后四王 BossState 仍在。同一副本会话内掉线再进：若已入伙且议会未 DONE，劳尔等不应被错误地再结盟一次，阵营应仍是友好。全员离开或世界服务器重启后再进：**入伙允许丢失**（内存-only），登记即可，不要为此补 `WriteSaveDataMore`。从门口潜行到哈兰甲板：允许到达（本阶段无脚本门）；若有隐形墙，记进游戏结果，不要为此补 `LoadDoorData`。LittleWigs 四王 `engageId` 仍为 2093–2096。

---

## Task 3 — 友好船长：周轮换朗姆酒区事件

**目的：** 普通、英雄、非钥石史诗共用「进本后做当周事件」；做完才有一名友好船长。不做则三人全敌对并吃 257821。周历表名 **仍未钉死**。细节、坐标、dump 搜索步骤见 [细节-F2-友好船长周事件.md](细节-F2-友好船长周事件.md)。

**要改的文件与函数：**

- `freehold.h`：Task 2 已加的 Action / Data；增加 `NpcOtis = 129441`（头文件现有 `NpcLokhtosDarkbargainer = 12944` 是黑石深渊经典 NPC，**禁止**拿来当 Otis）。增加 `enum FreeholdCrewWeek`（只定义一次，见下「跨文件接口」）。**不要**在头文件写 `209` / `210` / `211` 当周历常量。在 enum 之后声明两条自由函数（见下）。
- `instance_freehold.cpp`：`struct instance_free_hold` **继续只定义在本 cpp**（约 27 行）。在该 struct 上实现成员 `GetActiveFreeholdCrewWeek() const` 与 `NotifyCrewEventComplete(uint32 captainEntry)`；同一 cpp 再定义 `freehold.h` 里那两条自由函数（见下）。把 dump **搜到的** `game_event` / holiday / worldstate 映射到 `FreeholdCrewWeek`。**禁止**写成 `sGameEventMgr->IsActiveEvent(209/210/211)`。**禁止** `week % 3`，不要读 MDT。搜不到 ID 时：**停下来问用户**，成员函数此时只返回 `CrewWeekNone`，不要填空闲号。`SetData`/`GetData` 读写 `DataFriendlyCaptain` 与 `DataCrewEventDone`（内存-only，见 F5）。
- `boss_council_o_captains.cpp`：全部事件 AI **只写在本文件**（见下「脚本落点」）。`boss_council_captain::DoAction`：处理 Jolly / Eudora / Raoul 三条 Action（现仅劳尔）。`captainRaoulActivated` 改为通用「自己是友好船长」标志，或按 entry 判断 `getFaction() == FactionFriendlyFake`。`CaptainEnterCombat` 里乔里友好分支已有、劳尔写了两遍：改成按阵营走一次，不要复制第三份 10.1 逻辑。
- **不要改** `freehold.cpp`。`AddSC_freehold()` **保持空**（Task 11）。禁止新建 cpp。不要给普通小怪 129602 等写 BossAI。

### 脚本落点（写死，禁止「或」）

事件 AI **只**放在 `src/server/scripts/KulTiras/FreeHold/boss_council_o_captains.cpp`，由已有的 `AddSC_boss_council_o_captains()` 注册。loader 已调用该函数（`kul_tiras_script_loader.cpp` 第 84 行），不要再造 loader。

**禁止**把事件 AI 放进 `freehold.cpp`：`AddSC_freehold()` 保持空，放进去永不注册。**禁止**为了登记这些 AI 去改 `AddSC_freehold`。二选一的结论是：**空 AddSC_freehold + 全部进 `boss_council_o_captains.cpp`**，不是「两个 cpp 都行」。

`AddSC_boss_council_o_captains` **追加**四行新 `RegisterCreatureAI`（原有四条 **保留**，不要删 `npc_rummy_mancomb`）。整份清单如下，照抄：

```cpp
void AddSC_boss_council_o_captains()
{
    RegisterCreatureAI(boss_council_captain);
    RegisterCreatureAI(npc_rummy_mancomb);
    RegisterCreatureAI(npc_blackout_barrel);
    RegisterCreatureAI(npc_captains_controller);
    RegisterCreatureAI(npc_freehold_crew_knuckleduster);
    RegisterCreatureAI(npc_freehold_murphy);
    RegisterCreatureAI(npc_freehold_otis);
    RegisterCreatureAI(npc_freehold_crew_brew);
}
```

Spell 脚本（`RegisterSpellScript` 等）保持原样，接在这份 Creature 清单后面。`RegisterCreatureAI` 的脚本名等于 struct 名；SQL 写在 **`creature.ScriptName`（按 guid，见本 Task SQL 节）**，禁止对 129547 做 `creature_template` 整表 UPDATE。

该表的核心用途是：把「谁写在哪个 cpp、谁负责 Register、SQL 绑哪个 ScriptName」钉成一张清单，避免喝酒 NPC 漏注册。

| C++ struct（= `creature.ScriptName` 字符串） | 绑谁 | 职责 |
|-------------------------------|------|------|
| `npc_freehold_crew_knuckleduster` | 仅 guid **280003428** 与 **280003429** 的 129547 | 进战召唤 4 只 129547（`TEMPSUMMON_CORPSE_DESPAWN`）；两只事件体都死后且 `GetActiveFreeholdCrewWeek(me->GetInstanceScript()) == CrewWeekBlacktooth` 才 `NotifyCrewEventComplete(me->GetInstanceScript(), uint32(FreeholdCreature::NpcCaptainRaoul))` |
| `npc_freehold_murphy` | **130467** | `ScriptedAI`，override `sGossipHello`（`UnitAI.h` 约 298 行）。简化版，注释标明不是完整寻路嗅探。当周不是破浪则 gossip 不入伙 |
| `npc_freehold_otis` | **129441** | 同上 gossip。完成破浪时 `NotifyCrewEventComplete(me->GetInstanceScript(), uint32(FreeholdCreature::NpcCaptainJolly))`。禁止用 12944 |
| `npc_freehold_crew_brew` | 当周喝酒 NPC（**entry 仍未钉死**，见 F2 第 4 节） | gossip 必须 `GetActiveFreeholdCrewWeek(me->GetInstanceScript()) == CrewWeekBilgeRats` 才给点击者 **281357**。instance 统计本图存活玩家都有该光环后 `NotifyCrewEventComplete(..., NpcCaptainEudora)`。**即使 entry 还在问用户，本 struct 与 `RegisterCreatureAI` 也必须写上**；SQL 绑 ScriptName 等用户确认 entry 再写 |

不要给 dump 里 8 只普通 129547 绑 `npc_freehold_crew_knuckleduster`。Murphy / Otis / 喝酒 NPC 不要另开 `CreatureScript` 子类（那套不会走这份 `RegisterCreatureAI` 清单）。

### 跨文件接口（照抄能编）

`struct instance_free_hold` **不要**搬进头文件。**不要**改 `InstanceScript.h` 给基类加虚函数（铁律 1，核心管道）。外部翻译单元通过 `freehold.h` 的自由函数调用；自由函数在 `instance_freehold.cpp` 里对 `GetInstanceScript()` 的结果做 `dynamic_cast<instance_free_hold*>`（仓库已有 `dynamic_cast` 先例，RTTI 开着）。调用点**不要**自己写 cast。

在 `freehold.h` 于现有 enum 之后、文件末尾之前，照抄（`FreeholdCrewWeek` 只出现这一次）：

```cpp
class InstanceScript;

enum FreeholdCrewWeek : uint32
{
    CrewWeekNone = 0,
    CrewWeekCutwater = 1,
    CrewWeekBlacktooth = 2,
    CrewWeekBilgeRats = 3
};

FreeholdCrewWeek GetActiveFreeholdCrewWeek(InstanceScript const* instance);
void NotifyCrewEventComplete(InstanceScript* instance, uint32 captainEntry);
```

在 `instance_freehold.cpp` 的 `struct instance_free_hold` 内声明并实现两个**无 InstanceScript\* 参数**的成员（映射周历、按 F2 第 5 节 SetData + 三船长 `DoAction` + 改该帮小怪阵营）。同一 cpp、`AddSC_instance_freehold` **之前**定义自由函数，照抄：

```cpp
FreeholdCrewWeek GetActiveFreeholdCrewWeek(InstanceScript const* instance)
{
    if (instance_free_hold const* fh = dynamic_cast<instance_free_hold const*>(instance))
        return fh->GetActiveFreeholdCrewWeek();
    return CrewWeekNone;
}

void NotifyCrewEventComplete(InstanceScript* instance, uint32 captainEntry)
{
    if (instance_free_hold* fh = dynamic_cast<instance_free_hold*>(instance))
        fh->NotifyCrewEventComplete(captainEntry);
}
```

`boss_council_o_captains.cpp` 已 `#include "freehold.h"`。调用点照抄（不要猜 SetData 协议、不要在调用点 `dynamic_cast`）：

```cpp
if (GetActiveFreeholdCrewWeek(me->GetInstanceScript()) != CrewWeekBilgeRats)
    return;
NotifyCrewEventComplete(me->GetInstanceScript(), uint32(FreeholdCreature::NpcCaptainEudora));
```

`me->GetInstanceScript()` 返回 `InstanceScript*`，与声明一致。搜不到周历 ID 之前，成员 `GetActiveFreeholdCrewWeek()` 只返回 `CrewWeekNone`；`NotifyCrewEventComplete` 仍可先写，供 GM 直接调入伙。

**实施顺序（周历未决，禁止跳过）：**

1. 按 F2 第 2 节在 dump 搜 8.0 自由镇 `game_event` / holiday / worldstate / `game_event_creature`×map=1754。
2. 搜到：把 ID 写进成员 `instance_free_hold::GetActiveFreeholdCrewWeek()` 与 SQL 绑定，文件头注明 dump 出处。
3. 搜不到：**停下来问用户**。在此之前不要 INSERT `game_event`，不要写 `IsActiveEvent(209)`。`NotifyCrewEventComplete` / 三条 `DoAction` 仍可先写，GM 可直接调入伙做议会验收。
4. 事件 NPC 的 INSERT 依赖第 2 步的 ID（否则无法绑「当周才出现」）。拉米常驻刷怪不依赖周历 ID，与 Task 10 共用同一行。

**枚举 / ID：**

| 当周帮派 | 友好船长 | 玩家做什么 | 周历 ID |
|----------|----------|------------|---------|
| 破浪 | 乔里 126845 | 追 Murphy **130467**，开笼子 GO **278291**，与 Otis **129441** 对话 | **未决**（dump 搜到再填） |
| 黑齿 | 劳尔 126847 | 打两只高亮事件体 129547（会再招 4 只），不是随便打本内 8 只普通 129547 | **未决** |
| 水鼠 | 尤朵拉 126848 | 全队与**当周才刷**的喝酒 NPC 对话，吃 **281357** | **未决** |

dump `game_event` 最高 200，玛乌尔补丁占 201–208，209–211 **空闲不等于 8.3 真相**。禁止把这三个号写进 C++ 或 INSERT。

刷怪 guid 预留 **280003425–280003430**（dump 已核：1754 最后一行 280003424，这六号空闲；**禁止** 280004004 等地图 1864 占用号）。拉米 guid **280003427**，Task 10 交叉引用，禁止第二份 INSERT。

入伙成功后将该帮小怪改 `FactionFriendlyFake`（2580）。帮派 entry 名单见 F2。本内 8 只普通 129547 只有在黑齿入伙后才变友好，禁止再当入伙条件。

**禁止事项：** 禁止 `countRaoul`。禁止 10.1 自动结盟。禁止 MDT `week % 3`。禁止门口三选一 gossip。禁止 `id=129547000`。禁止用 12944 当 Otis。禁止把钥石周表抄进普通。禁止做完事件还不改船长阵营却只改小怪。禁止 `IsActiveEvent(209/210/211)`。禁止复用常驻 **127124** 当喝酒狐人，禁止给 127124 加不限当周的 gossip。禁止 guid 280004001–280004010。

**SQL：** `2026_08_30_10_world_freehold_crew_spawns.sql`：拉米 **280003427** 常驻一行（与 Task 10 同一只）；Murphy / Otis / 两只事件体 / 喝酒 NPC 仅在周历 ID 搜到（或用户确认）之后插入并绑定。`2026_08_30_11_world_freehold_crew_scriptnames.sql`：本仓库 `creature` 表有 **`ScriptName` 列**（`ObjectMgr.cpp` 约 2057–2058 行 `LoadCreatures` 的 SELECT 含 `creature.ScriptName`；按 guid 加载，空则回退 `creature_template.ScriptID`）。**禁止** `UPDATE creature_template SET ScriptName=... WHERE entry=129547`（会误绑本内 8 只普通指节手）。照抄：

```sql
UPDATE `creature` SET `ScriptName`='npc_freehold_crew_knuckleduster' WHERE `guid` IN (280003428, 280003429);
UPDATE `creature` SET `ScriptName`='npc_freehold_murphy' WHERE `guid`=280003425;
UPDATE `creature` SET `ScriptName`='npc_freehold_otis' WHERE `guid`=280003426;
-- 下一行仅在用户确认喝酒 NPC entry 并已 INSERT guid 280003430 之后执行：
UPDATE `creature` SET `ScriptName`='npc_freehold_crew_brew' WHERE `guid`=280003430;
```

这四条 UPDATE 必须出现在对应 guid 的 INSERT 之后（同一文件后半或 11 号文件均可，禁止先 UPDATE 再 INSERT）。**不要**在本 Task 发明 `game_event` 209–211 行。

**编译：** 同铁律第 17 条。

**完成定义：** 周历 ID 要么来自 dump 出处注释，要么有「已问用户、用户给定 ID」的记录，**不能**是方案里的 209–211。`freehold.cpp` 无事件 AI；`AddSC_freehold` 仍为空；`AddSC_boss_council_o_captains` 含上表四个新 `RegisterCreatureAI`（含 `npc_freehold_crew_brew`）。`freehold.h` 有 `GetActiveFreeholdCrewWeek(InstanceScript const*)` 与 `NotifyCrewEventComplete(InstanceScript*, uint32)` 声明；`boss_council_o_captains.cpp` 经这两条自由函数调用，没有把 `GetGuidData` 当周历、没有在调用点 `dynamic_cast`。三套事件 NPC 仅当周出现；完成事件后对应船长为 2580，另外两名仍为敌对 16；未做事件时三名都是敌对；`OnUnitDeath` 不再数普通 129547；普通/英雄/史诗走同一套代码，无「仅钥石才事件」分支。1754 只有一只 133219（guid 280003427）。127124 无入伙 gossip。

**验收（待用户进游戏）：** 用**搜到的**事件 ID（或用户给定的 ID）轮流开三周，各做一次事件，CLEU / 目标框确认一名船长 `UnitIsFriend`。不做事件则三人都可攻击且有 257821。非水鼠周不能靠 127124 入伙尤朵拉。LittleWigs 议会模组能识别友好者（它只检测敌友，不检测选帮 NPC）。Murphy / Otis / 高亮指节手 / 喝酒 NPC 的坐标若需微调，只改 SQL 坐标，不改 entry。

---

## Task 4 — 议会 DONE：两名敌对即结束；257821 保持 +100%

**目的：** 留一名友好时打死两名敌对就能 `ENCOUNTER_END` 2094 并出 288636。257821 系数不动。详见 F3 议会节与 F1 叠层节。

**要改的文件与函数：**

- `boss_council_o_captains.cpp` → `npc_captains_controller::UpdateAI`：把 `captainsDeathCount > 2` 改成 **`captainsDeathCount >= 2`**（两名敌对死亡即 DONE）。`JustDied` 仍只给控制器 `ACTION_COUNT_DEATHS`，友好船长不应走敌对死亡（友好不被玩家打死；若误伤，不要计数——`JustDied` 里若 `getFaction() == FactionFriendlyFake` 则不 `DoAction`）。
- `GetController()`：必须返回 `Creature*`。现行是 `me->FindNearestCreature(NpcCaptainsController, 500.0f)`（约 373–375 行），要换掉。照抄同文件 `EnterEvadeMode` 约 287 行已有包装，**禁止**把 `GetGuidData` 的返回值当成 `Creature*`：

```cpp
Creature* GetController()
{
    if (!instance)
        return nullptr;
    return ObjectAccessor::GetCreature(*me, instance->GetGuidData(DataCaptainsController));
}
```

`ObjectAccessor::GetCreature` 在 `ObjectAccessor.h` 第 74 行：`Creature* GetCreature(WorldObject const& u, ObjectGuid const& guid)`。本文件已 `#include "ObjectAccessor.h"`。`instance->GetGuidData` 走 `instance_free_hold` 的 override（成员 `captainsControllerGuid`，`instance_freehold.cpp` 约 179–180 行）。三只 160000 按 `spawnDifficulties` 分难度刷，当前难度实例里应只有一只。

禁止：
- `return instance->GetGuidData(DataCaptainsController);`（类型是 `ObjectGuid`，编不过）
- `instance->GetCreature(DataCaptainsController)` 或 `instance->GetCreature(uint32(DataCaptainsController))`：这是 `InstanceScript::GetCreature(uint32)`（`InstanceScript.h` 第 256 行；实现 `InstanceScript.cpp` 第 191–194 行，内部 `GetObjectGuid` 读 `_objectGuids`）。自由镇没有 `LoadObjectData`，这条路径控制器**恒空**，`>= 2` 永远不会 DONE
- 不要继续用 `FindNearestCreature(..., 500.f)` 当最终实现

说明：`instance_freehold.cpp` 里 `instance->GetCreature(raoulGuid)` 能用，是因为那边的 `instance` 是 `InstanceMap*`（`Map::GetCreature(ObjectGuid)`）。`BossAI` 里的 `instance` 是 `InstanceScript*`，同名成员不是同一套 API。GetController **写死** ObjectAccessor 包装，不要改写成 Map 那套、也不要改写成 `InstanceScript::GetCreature(uint32)`。
- `checkFaction()`：三人全是 `FactionEnemy` 才 `CastSpell(me, UnderOneBanner 257821, true)`。已入伙则三人都 `RemoveAura(257821)`。不要改 257821 的 SpellEffect。
- `EventCheckPlayers`：保持灭团检测；不要在这里「补 DONE」以外的第三条死亡。注释里「友好时永远不会正常结束」在 `>= 2` 修好后改掉，避免后人再抄错。

**枚举 / ID：** `ACTION_COUNT_DEATHS=1`。GO **288636**，召唤坐标已有 `CouncilTributePos = {-1784.44f, -684.403f, 38.517f, 3.15363f}`。Data25 模板已是战斗遭遇 **2094**，不要改成手册 2093。Data30=86063 不要读。箱子 loot 是 Task 8。

**禁止事项：** 禁止改回 `> 2`。禁止三人全死才出箱。禁止把 257821 改成伤害 +25%。禁止在本 Task 改掉落表。禁止静态补刷 288636（C++ 已召唤；Ashamane 静态行不是本阶段必做）。

**SQL：** `2026_08_30_19_world_freehold_council_text.sql`：乔里 126845 / 尤朵拉 126848 / 劳尔 126847 的 `creature_text` 按 F3 第 6 节对齐 C++ Talk 编号（列名 `CreatureID` / `GroupID`，不是 creatureId）。箱子 loot 仍是 Task 8。

**编译：** 同铁律第 17 条。

**完成定义：** 友好 + 打死两名敌对 → `SetBossState(DataCounciloCaptains, DONE)` + 召唤 288636；`captainsDeathCount` 在友好存活时能到 2；`GetController()` 经 `ObjectAccessor::GetCreature(*me, instance->GetGuidData(DataCaptainsController))` 取控制器，全文件搜不到 `instance->GetCreature(DataCaptainsController)`；257821 的施法条件仍是三人全敌对；Spell 257821 本仓库无自定义改系数。三船长 `creature_text` Group 与 C++ Talk 对齐到 F3 第 6 节。

**验收（待用户进游戏）：** 入伙后打两名敌对，CLEU 出现 `ENCOUNTER_END` **2094**，地上出现议会贡品。三人全敌对时 257821 上身，上身前后最大生命约翻倍（叠在 Task 1 之后的基础生命上）。普通也要测全敌对（惩罚不是史诗独有）。

---

## Task 5 — 克拉格：256056、座骑 126841、俯冲英雄+

**目的：** 75% 切阶段的战斗日志锚点是 256056；座骑 AI 跑在 126841 上；俯冲 272046 在英雄与史诗放、普通不放。详见 F3 克拉格节。

**要改的文件与函数：**

- `boss_skycapn_kragg.cpp`：
  - `enum KraggSpells` 增加 `SpawnParrot = 256056`。
  - `boss_skycap_kragg::DamageTaken`：在 75% 且 `phase == PhaseMount` 时设 `phase = PhaseUnmount`，`Talk(TalkUnmount)`，**只** `me->CastSpell(me, KraggSpells::SpawnParrot, false)`。不要在这里 `ExitVehicle`。
  - `void OnSpellCasted(SpellInfo const* spellInfo) override`（`CreatureAI.h` 约 263 行；`Spell.cpp` 约 3443 行在成功施法后调用。仓库**没有** `OnSpellCast`）：仅当 `spellInfo->Id == SpawnParrot` 时 `ExitVehicle`，让已有 126841 `MovePoint(MiddlePos)`，再排二阶段事件。这是下坐骑的**唯一**地点。禁止再 override `OnSuccessfulSpellCast`（约 291 行，同一 `finish()` 会再调一次）。禁止用 `SpellHit` / 座骑 `IsSummonedBy` 切阶段。
  - 给 `spell_spawn_parrot` 写 SpellScript：`OnEffectHit` 对效果 28 若 `GetGuidData(NpcSharkBaitBoss)` 已有生物则 `PreventHitDefaultEffect`。`Reset` 开战前已有 126841，必须挡默认召唤。禁止两只战斗座骑。SpellScript **不**负责 `ExitVehicle`。
  - `npc_sharkbait::MovementInform` 的 `MovementPointMiddle`：把 `if (IsHeroic())` 改成 `if (IsFreeholdHeroicPlus(me->GetMap()))` 再 `ScheduleEvent(EventDiveBombs, 17000)`。普通不排。
- `freehold.h`：`NpcSharkBaitBoss = 126841` 已有；`NpcSkycapKraggRider = 126732` 在 dump 是 Pinky，本 Task **不要**拿 126732 / 129732 当座骑。
- SQL：`creature_template` entry **126841** 的 `ScriptName='npc_sharkbait'`。**不要**改 129743 的 ScriptName（该模板刷在地图 603，不在 1754）。126841 有 SAI 放 256035；有 ScriptName 后核心忽略 SAI（`CreatureAISelector.cpp:37-40`），C++ 放 **256005**（插件听的邪恶轰炸）。

**枚举 / ID：** 126832 王；126841 战斗座骑；256056 切阶段；272046 俯冲（手册段 18451 只挂难度 2 与 23）；256005 / 256035 都叫 Vile Bombardment，战斗座骑用 C++ 的 256005。手册遭遇 2102，战斗遭遇 2093。

**禁止事项：** 禁止只抄 75% `ExitVehicle` 不下发 256056。禁止写 `void OnSpellCast(`（符号不存在）。禁止把完成定义理解成「搜子串 OnSpellCast」而去改或删现有 `OnSpellCastInterrupt`（约 197 行必须保留）。禁止 `OnSpellCasted` 与 `OnSuccessfulSpellCast` 双挂。禁止把 `npc_sharkbait` 绑到 129743 当本图战斗座骑。禁止补刷 129732 当战斗实体。禁止 `ScriptedAI::IsHeroic()` 控制俯冲。禁止普通难度排 `EventDiveBombs`。禁止解注释 `SetBossState(FAIL)`（标注会崩核心，本阶段不做）。禁止为俯冲补 SPELL_EFFECT_254 核心效果。

**SQL：** `2026_08_30_12_world_freehold_kragg_sharkbait.sql`（UPDATE 126841 ScriptName；可选：126832 `creature_text` Group 6–8，英文即可，35662 中文 Spell 字符串本源未钉）。`spell_script_names` 插入 `(256056, 'spell_spawn_parrot')`（本 Task 必做，不是可选）。

**编译：** 同铁律第 17 条。

**完成定义：** 全文件 75% 路径必 `CastSpell(256056)`；下坐骑只在 `OnSpellCasted`。验收搜索词写死为：全文件搜不到独立钩子 **`void OnSpellCast(`** 或作为函数名的 **`OnSpellCast(`**（不要搜子串 `OnSpellCast`，那会误伤下面两个符号）。**必须保留**现有 `OnSpellCastInterrupt`（`boss_skycapn_kragg.cpp` 约 197 行，打断回春酒；`CreatureAI.h` 约 288 行）。切阶段钩子仍是 `OnSpellCasted`（`CreatureAI.h` 约 263 行）。禁止再 override `OnSuccessfulSpellCast`。126841 `ScriptName` 为 `npc_sharkbait`；俯冲调度用 `IsFreeholdHeroicPlus`；搜不到对 129732 的 `SummonCreature`；无第二只座骑。

**验收（待用户进游戏）：** 英雄与史诗二阶段 CLEU 有 `SPELL_CAST_SUCCESS` **256056**（约 75% 附近）以及 **272046**；普通无 272046。LittleWigs `Kragg.lua` 能切阶段 2。门口**不应**为了本 Task 出现第二只库拉格 129732。邪恶轰炸 CLEU 施法者是 126841。

---

## Task 6 — 环赌：扔鱼饵英雄+、双托萨克、抛鲨载具 API

**目的：** 272534 在英雄与史诗放、普通不放；入场不要第二只托萨克；抛鲨读自己的载具乘客。详见 F3 环赌节。过场链（戴夫 → 猪 → 龟 → 托萨克）已通，不要跳过场。

**要改的文件与函数：**

- `boss_ring_of_booty.cpp` → `boss_trothak::MovementInform`（`MovementPointFight`）：`if (IsHeroic())` 改 `if (IsFreeholdHeroicPlus(me->GetMap()))` 再调度 `EventsThrowChum`。
- 同文件 `npc_shark_trothak` 构造函数：`if (IsHeroic())` 改同一宏（鱼饵地面 AT 检查）。
- `npc_gurgthock::DoAction` 的 `ActionThirdEvent`：不要 `SummonCreature(NpcTrothak)`。改为 `GetGuidData(NpcTrothak)` 取地图静态 126969，`Respawn`（若死）后 `MovePoint(TrothakMovePos[...])`。`trothakGuid` 以 OnCreatureCreate 为准。
- `boss_trothak::UpdateAI` 的 `EventsSharkToss`：把 `me->GetVehicle()` 改成 **`me->GetVehicleKit()`**（自己是载具 5658，不是乘客）。乘客座位 0/1 对应锤头鲨 129448 / 锯齿鲨 129359。
- `boss_trothak::Reset`：保持召唤鲨鱼那段**注释掉**。只走 SQL `vehicle_template_accessory`。静态地面 129448/129359：Reset 里若自己已是载具且有乘客，则 `DespawnOrUnsummon` 附近静态同 entry，避免双鲨。禁止解注释 SummonCreature 再叠一套。

**枚举 / ID：** 126969；VehicleId **5658**；扔鱼饵 **272534**（手册段 18469，难度 2 与 23）；Rearm 保持 **256489**（256706 是伤害，进游戏对照，本阶段不换施法 ID）。战斗遭遇 2095，手册 2094。猪点击保持 `countClick >= 5` / Greasy 257829（3 次来自零售页，不改）。

**禁止事项：** 禁止跳过场。禁止 `ScriptedAI::IsHeroic()` 控制 272534。禁止普通排扔鱼饵。禁止 Gurgthock 再召唤第二只 126969。禁止观众砸食物 268287（未决，本阶段不做）。禁止把插件旋风 23 秒/20.4 秒写进 EventMap。

**SQL：** `2026_08_30_13_world_freehold_trothak_vehicle.sql`：dump 表 `vehicle_template_accessory` 列为 `entry, accessory_entry, seat_id, minion, description, summontype, summontimer`，其中 **`description` NOT NULL 且无默认值**。INSERT 必须带全部列，例如 `(126969, 129448, 0, 1, 'Trothak - Hammerhead Shark', 6, 30000)` 与 `(126969, 129359, 1, 1, 'Trothak - Sawtooth Shark', 6, 30000)`。座椅 ID 以 35662 载具 5658 为准，对不上只改 `seat_id` 不改 entry。不要改 dump 里已有静态鲨的 guid，用脚本 despawn。

**编译：** 同铁律第 17 条。

**完成定义：** 全文件扔鱼饵判定为 `IsFreeholdHeroicPlus`；`ActionThirdEvent` 无 `SummonCreature(126969)`；抛鲨走 `GetVehicleKit()`；配件表 INSERT 含 `description`；Reset 召唤保持注释。

**验收（待用户进游戏）：** 必须点戴夫开过场，不能直接打静态托萨克开战。英雄/史诗 CLEU 有 272534，普通无。开战只有一只 126969。抛鲨时鲨鱼离开座位扑人。LittleWigs `engageId=2095`。入场火焰 Todo 允许仍缺，记偏差即可。

---

## Task 7 — 哈兰：掷弹兵读玩家；台词与 SQL 对齐

**目的：** 火药兵能贴到被点名的玩家身上引爆；死亡不再喊炮击句。详见 F3 哈兰节。

**要改的文件与函数：**

- `boss_harlan_sweete.cpp` → `npc_irontide_granadier::UpdateAI`：`ObjectAccessor::GetCreature(*me, targetGUID)` 改为 **`ObjectAccessor::GetUnit(*me, targetGUID)`**（或 `ObjectAccessor::GetPlayer`）。`targetGUID` 来自 `SelectTarget` 的玩家。距离 ≤3 码再 `CastSpell(BlackPowderBomb 257315)`。
- 不要给 129758 再挂 SAI（ScriptName 已优先）。
- 不改 C++ `HarlanTalk` 数字。SQL `creature_text` 按枚举对齐：0 进战、1 六十%（All Hands）、2 死亡、3 掷弹兵、4 炮击、5 三十%（Man-O-War）。把 dump 里错位的 Group 1–3 改到正确语义，补 4 与 5。议会三船长台词不在本 Task（Task 4 的 `2026_08_30_19`）。

**枚举 / ID：** 126983；掷弹兵 129758；军刀 129747；257402 / 257458 阶段骰子（60% / 30%）已有，不要加 413xxx。战斗遭遇 2096，手册 2095。火药光环 257314。

**禁止事项：** 禁止现役 LittleWigs master 的 413131 / 413136。禁止把「海盗来了」84.4 秒写进 EventMap。禁止本 Task 删钥石 loot（那是 Task 8）。禁止史诗新技能分支（8.3 哈兰没有第四套技能）。

**SQL：** `2026_08_30_14_world_freehold_harlan_text.sql`（只动 126983 的 `creature_text`）。

**编译：** 同铁律第 17 条。

**完成定义：** 掷弹兵近距判定对玩家有效；`GetCreature` 不再吃这个 GUID；`creature_text` Group 与 `HarlanTalk` 一一对应到 5。

**验收（待用户进游戏）：** 被点名玩家被火药兵追，3 码内爆炸 CLEU 257315。死亡台词不是炮击句。LittleWigs 手册 2095 / engage 2096。阶段 2 弹幕打多人（已有 All Hands 分支），不要为成就 12833 强制打箱子。

---

## Task 8 — 掉落：清脏数据、议会箱、坐骑史诗条件

**目的：** 三难度仍共用一张生物掉落表；开箱靠 ItemContext；议会箱能开出议会装；节日帽和钥石消失。详见 [细节-F4-掉落与Encounter.md](细节-F4-掉落与Encounter.md)。

**要改的文件：** 仅 SQL，不改 C++，不改 ItemSparse，不拆表。

- `2026_08_30_15_world_freehold_loot_cleanup.sql`：
  - `DELETE` `creature_loot_template` 中 `(126832, 139299)`（节日帽 Chance 53）。
  - `DELETE` `(126983, 158923)`（神话钥石 Chance 5）。不要做钥石系统。
  - `conditions`：`CONDITION_SOURCE_TYPE_CREATURE_LOOT_TEMPLATE=1`，SourceGroup=126983，SourceEntry=**159842**，`CONDITION_DIFFICULTY_ID=49`，ConditionValue1=**23**。Chance 保持 21。
- `2026_08_30_16_world_freehold_council_chest.sql`：
  - `UPDATE gameobject_template SET Data1=288636 WHERE entry=288636`（现 Data1=0）。
  - `INSERT gameobject_loot_template` Entry=**288636**，物品为 ATT 议会块八件（Chance 用与其它王装备同量级的 2–8，F4 写死每一行）：159132、159130、158311、159356、158346、159297、158351、158314。三船长 `lootid` 保持 0。
  - 不要读 Data30=86063。不要改 Data25=2094。

**枚举 / ID：** 加成树 1492（上下文 17→4776、2→4778、23→4779）；选择器树 1545 仍是 340/355/370，本阶段不改选择器去凑赛季 400。本图普通 ItemContext **17**，4776 保留。

**禁止事项：** 禁止拆成三张 `creature_loot_template`。禁止删 4776。禁止改 159633 物品 ID 或底装等。禁止 GM 发装冒充掉落。禁止动 129732 那张重复 loot 表当一王掉落（129732 本图未刷）。

**编译：** 无（纯 SQL）。

**完成定义：** 世界库无 139299@126832、无 158923@126983；288636 有 loot 行且 Data1=288636；159842 带难度 23 条件；仍各只有一张生物掉落表。

**验收（待用户进游戏）：** 普通/英雄/史诗各打一次克拉格与哈兰开箱，看物品等级（普通应能套到 4776 相关加成；不要期望一定是 400/415/430）。议会 DONE 后开 288636 有议会装。普通/英雄哈兰不开出 159842；史诗可以。三难度都不应开出节日帽或钥石。

---

## Task 9 — Encounter 手册绑定

**目的：** 手册表与战斗遭遇表不再混用。编号表见 F4。

**要改的文件：** 仅 SQL。

- `2026_08_30_17_world_freehold_journal.sql`：
  - `UPDATE creature_template_journal SET JournalEncounterID=2093 WHERE entry=126848`（现错绑 **2094**；2094 是手册「战利品环」）。dump 列名是 **`entry`** 与 **`JournalEncounterID`**，禁止写成 `creatureId` / `journalEncounterId`。
  - `INSERT INTO creature_template_journal (entry, JournalEncounterID) VALUES (126969, 2094)`（手册环赌；本源缺行）。
  - 126832→2102、126983→2095 已正确，不要动。
  - **禁止 UPDATE** `instance_encounters`：`(2093,126832)` `(2094,126848)` `(2095,126969)` `(2096,126983)` 已与 LittleWigs engage 一致。

**枚举 / ID：** 战斗 2093–2096；手册 2102/2093/2094/2095。DungeonEncounter 2102 是托尔达戈的 Jes Howlis，禁止写进自由镇 `instance_encounters.entry`。

**禁止事项：** 禁止用 MDT 2095 改克拉格。禁止把手册号写进 `instance_encounters`。禁止改 GO 288636 的 Data25。

**编译：** 无。

**完成定义：** 尤朵拉手册 2093；托萨克手册 2094；四条 `instance_encounters` 字节不变。

**验收（待用户进游戏）：** 冒险指南四王页能点到对应 NPC。LittleWigs `NewBoss` 第三参仍是 2102/2093/2094/2095，engage 2093–2096。DBM `SetEncounterID` 仍 2093–2096。无插件可测手册表时，以 SQL 查询结果为准并在回复里写「未进游戏」。

---

## Task 10 — 酒保拉米刷怪与丢酒接线

**目的：** 133219 出现在议会现场；英雄/史诗开战丢酒。普通是否丢酒以 DBM「非普通才循环」为**本阶段默认**；进游戏若普通 CLEU 有酒，只改判定一行。详见 F3 酒保节。

**要改的文件与函数：**

- SQL 刷 133219：guid **280003427**（F2 / Task 3 已核空号），坐标 **(-1780.0, -678.0, 38.52)**（议会贡品 `CouncilTributePos` 旁，area 9164，`spawnDifficulties='1,2,23,8'`）。模板 ScriptName 已是 `npc_rummy_mancomb`，不要改名。不挂周事件（三周都在场）。**禁止** guid 280004003 / 280004010。**禁止**本 Task 再 INSERT 第二只；刷怪行写在 `2026_08_30_10_world_freehold_crew_spawns.sql`（Task 3 那份），本 Task 只做 C++ 接线。
- `boss_council_o_captains.cpp` → `boss_council_captain::EnterCombat`：在 `CaptainEnterCombat()` 之后（约 312 行之后），若 `IsFreeholdHeroicPlus(me->GetMap())`，**只**用 `FindNearestCreature` 找拉米并开机。写死下面这一段，禁止第二条路径：

```cpp
if (IsFreeholdHeroicPlus(me->GetMap()))
{
    if (Creature* rummy = me->FindNearestCreature(uint32(FreeholdCreature::NpcRummyMancomb), 80.f))
        rummy->AI()->DoAction(CouncilCaptainAction::ActionStartLaunchBrew);
}
```

`FindNearestCreature` 在 `Object.h` 第 524 行：`Creature* FindNearestCreature(uint32 entry, float range, bool alive = true) const`。`NpcRummyMancomb = 133219` 已在 `freehold.h` 第 44 行。`ActionStartLaunchBrew` 已在本文件 `CouncilCaptainAction`（约 88 行）。现在全仓库无外部调用，酒数组不会跑。

禁止走 GUID：`instance_free_hold::OnCreatureCreate` **不**记 133219，`GetGuidData` 没有拉米分支。本 Task **不要**改 `instance_freehold.cpp`、**不要**新增 `rummyGuid`、**不要**把 `instance_freehold.cpp` 列入本 Task 文件清单。禁止 `instance->GetCreature(uint32(NpcRummyMancomb))`（`InstanceScript::GetCreature(uint32)`，空 `_objectGuids`，恒空）。本 Task C++ 只改 `boss_council_o_captains.cpp`。
- `npc_rummy_mancomb::Reset` 保持 `REACT_PASSIVE` + `NOT_SELECTABLE`。`HeroicSpell[3]` 保持 **265086 / 264715 / 265171**（35662 SpellName 与插件听的 265088 / 264608 / 265168 同名不同 ID，都是真技能；C++ 继续放 Haven 数组，验收 CLEU 两套都看）。

**枚举 / ID：** 133219；`ActionStartLaunchBrew`；264605 调酒吧驱动插件在听，本阶段不强制另写 264605，除非进游戏完全无酒。

**禁止事项：** 禁止普通默认丢酒（除非用户进游戏打脸本默认）。禁止把酒保做成选帮 gossip。禁止 10.1 删事件后的行为。禁止用 instance GUID / `GetGuidData` / `InstanceScript::GetCreature(uint32)` 找 133219。

**SQL：** 不要单独 `2026_08_30_18_world_freehold_rummy_spawn.sql`。拉米 INSERT 只出现在 Task 3 的 `2026_08_30_10`。

**编译：** 同铁律第 17 条。

**完成定义：** 1754 有且仅有一行 `id=133219` 且 guid=280003427；议会 `EnterCombat` 在英雄+用 `me->FindNearestCreature(uint32(FreeholdCreature::NpcRummyMancomb), 80.f)` 再 `DoAction(CouncilCaptainAction::ActionStartLaunchBrew)`；普通不调用；本 Task 未改 `instance_freehold.cpp`，没有 `rummyGuid`。

**验收（待用户进游戏）：** 英雄/史诗议会 CLEU 出现三酒之一（265086 或 265088 等）。普通登记有无。DBM 非普通调酒条能对上即可。

---

## Task 11 — 小怪 SAI 保持；以及本阶段明确不做

**目的：** 把调研 G/H/K 与未决里「不挡能打通」的项钉死为不做，避免实施员自行加戏。

**要改的文件与函数：** `freehold.cpp` 的 `AddSC_freehold()` **保持空**。`enum FreeHoldTrashSpells` 可留作注释，但其中 257246 在 35662 是还魂、SAI 实际放 **257426**——不要把错误 ID 写成 Cast。不要给执行者/诱捕者新建 CreatureAI。

**枚举 / ID（明确不要当漏怪补刷）：** 水鼠盐鳞 **129600** 本内已有 9 只。捕鼠陷阱 **274383** SpellEffect 只有 DUMMY，不强制 BossAI；地面无夹子则另立项，不在本阶段。

**本阶段明确不做（完整清单）：**

| 项 | 原因 |
|----|------|
| 钥石难度 8、词缀、S4 觉醒尖塔、`ChallengeModeMgr` case 1754 | 分类 K |
| 拆 `spawnDifficulties` 里的 8 | 修三难度不要拆 8 |
| GO 246779 力量之源 | 史诗门口误植，K |
| 10.1 打完库拉格自动结盟 | 零售弃用 |
| MDT week%3 当普通/英雄选帮 | R3 第 4 条 |
| 现役 413xxx、复苏之酒仅 50%、257821 现页 +25% | 零售弃用 |
| `LoadDoorData` / override `CheckRequiredBosses` 做成前三 DONE 才能打哈兰 | 本阶段不做顺序门；与社区跳尾王刷坐骑冲突，F5 只记录 |
| 补刷 129732 过场库拉格、129743 世界鲨鱼饵 | 129732 不是 256056 座骑；129743 在地图 603 |
| 补刷 129600 | 已有 9 只 |
| 点名/手雷/决斗冲刺写 C++ 生物 AI | SpellEffect+SAI 已够；R3 第 11 条 |
| 小怪 waypoint | 本源未见，进游戏验收站桩 |
| `allowMount`、普通准入 110、9164 墓园、`lfg_dungeon_template` | 进游戏验收，不挡能打通 |
| 克拉格 `SetBossState(FAIL)` 解注释 | 标注会崩核心 |
| 俯冲 SPELL_EFFECT_254 轨迹伤 | 文件头 Missing；简化版允许 |
| 环赌入场火焰、观众 268287 | 未决 / Todo |
| 猪点击改成 3 次 | 零售页 |
| 友好乔里补 257117 | 保留 281329；进游戏看是否两条都放 |
| 世界任务 52454/52456/52458 | 不算五人本最低集合 |
| 把 ContentTuning 500/501/502 接进 Haven 血量 | 现役主路径是 HealthModifier；本阶段只删错误硬乘 |
| HavenLab / `.lab` pack | 用户指定 |
| 改 Databases dump | 哲学 |
| Gukguk 130090 / Gurgthock 129350 过场 `creature_text` | 本源未见，不编台词 |
| 地下城成就 SQL（`achievement` / criteria；ATT 的 12548 / 12550 / 12831 / 12832 / 12833 / 12998） | 调研 I 数据缺；不挡能打出装 |

**SQL / 编译：** 无。若有人改了 `AddSC_freehold` 填小怪 BossAI，本 Task 视为失败。

**完成定义：** `AddSC_freehold` 仍为空；1754 没有新增 129600/129732 刷怪行；文档与代码都没有顺序门半截实现。Gukguk/Gurgthock 台词与地下城成就 SQL 已列入上表「明确不做」，没有另开 Task。

**验收（待用户进游戏）：** 小怪会放 SAI 里的 257426 等；诱捕者读条后夹子有无、129600 放 257784 还是 281420 只登记，不在本阶段改 C++。执行者不要有 DF 误植的 257732。

---

## 建议实施顺序

Task 1 → Task 2（含 `IsFreeholdHeroicPlus` 头文件）→ Task 9（纯 SQL，不挡）→ Task 8（纯 SQL）→ Task 5 → Task 3 → Task 4 → Task 10 → Task 6 → Task 7 → Task 11 核对。  
同一 `boss_council_o_captains.cpp` 上 Task 3/4/10 串行。Task 8 与 Task 9 可与 C++ 并行。

---

## 编译与提交

- 工作目录：`H:\wow837_projects\BFA-HavenCore`
- 命令：`cmake --build build --config RelWithDebInfo --target worldserver`
- 每个改了 C++ 的 Task 编译一次。纯 SQL 不编译。
- **本轮方案执笔不 commit。** 实施阶段一条 Task 一个提交；提交说明写行为来源（调研 R3 条号 / 35662 表 / Wowhead 弃用项）。

---

## 待用户进游戏验收总清单

实施员不进游戏。用户按本清单打勾。不要为这些项新建 HavenLab。

1. 同一模板生物：普通 / 英雄 / 史诗最大生命（Task 1；英雄不得 ×2，史诗不得 ×2.66）。
2. 一发白字与一发法术直伤（白字或仍乘 1.29/1.612，法术不应乘该表）。
3. 三难度各开一次箱子看物品等级与 4776 是否出现在普通（Task 8）。
4. 普通议会 CLEU 有没有酒保三酒；英雄/史诗应当有（Task 10）。
5. 用 dump 搜到（或用户给定）的周历 ID 各做完朗姆酒区事件后，对应船长友好；**不要**用 `.event start 209/210/211` 当正式手续（Task 3）。非水鼠周不能靠 127124 入伙尤朵拉。同一会话掉线再进入伙仍在；重启后允许丢失（Task 2 内存-only）。
6. 75% 附近 CLEU 是否有 256056；英雄/史诗有 272046，普通无（Task 5）。
7. 托萨克英雄/史诗有 272534，普通无；只有一只 126969（Task 6）。
8. 友好船长存活时 `ENCOUNTER_END` 2094；288636 有装（Task 4+8）。
9. 门口潜行到哈兰：无脚本门；记录有无隐形墙（Task 2）。
10. 三人全敌对 257821 上身，生命约翻倍（Task 4）。
11. 诱捕者夹子；129600 技能 257784 vs 281420（Task 11，只登记）。
12. 门口是否多一只 129732（Task 5 禁止补刷，此处确认没有）。
13. `allowMount=0`、普通准入 110、9164 墓园（明确不做代码，只登记）。
14. 静态鲨是否像载具乘客；猪 Greasy 层数（登记偏差）。
15. 友好乔里战斗里 257117 与 281329 是否都放（保留 281329）。
16. LittleWigs v8.3：四王 engage 2093–2096，手册第三参 2102/2093/2094/2095；俯冲条仅非普通。

WCL 8.3 分王秒数调研未打开，不要写成已经查过。

---

## 方案审查 R1

审查员未写过本方案、未改 C++。对照：[调研-自由镇.md](调研-自由镇.md)（R1+R2+R3 定稿）、细节 F1–F5、哲学「五人本」节，并抽查 `BFA-HavenCore` 源码与 dump。进游戏由用户做，本轮不验收通关。

**Status: Issues Found**

下表只列「实施员按方案原文动手会做错或漏掉调研已闭合缺口」的项。通过项见表格之后。

该表的核心用途是：按 Task 编号标出必须改方案再动手的点，避免把未闭合的周历表、错误 SQL 列名和会撞号的 guid 写进仓库。

| Task 编号 | 问题类型 | 按方案实施会做错的原因 |
|----------|----------|------------------------|
| **Task 3** | 与调研矛盾：发明了未闭合的 `game_event` ID | 调研 R3 第 4 条与「仍开放」清单写明：dump **没有**自由镇周历行，**服务端用哪张表尚未钉死**，实施不能假装已经有周历。方案把 C++ 写成 `sGameEventMgr->IsActiveEvent(209/210/211)`，等于把未闭合的表名和三个自造 `eventEntry` 写死。dump 里 `game_event` 最高到 200，玛乌尔补丁占 201–208，209–211 **确实空闲**，空闲不等于 8.3 真相。一旦本源不是 `game_event`、或正式号不是这三号，刷怪绑定与 `.event start` 验收会整本接错。 |
| **Task 3** | 函数/数据抽查：预留 guid 已占用 | 细节 F2 把 **280004001–280004010** 写成确定插入号。dump 里 **280004004、280004007、280004008、280004009、280004010** 已是地图 **1864** 的生物（例如 280004004 = 134139，280004010 = 134173）。方案虽写「撞号则 `MAX(guid)+1`」，但细节表把这些号当成可复制的 INSERT。照表执行会主键冲突，或先 DELETE 再插入时删掉另一张图的怪。 |
| **Task 3** | 与调研矛盾：喝酒 NPC 身份未钉死，且会打穿周闸 | 调研要的是当周**高亮狐人**对话喝酒，**没有**钉死 entry。方案把本内已有 **127124**（模板名 Freehold Barhand，guid **280003136**，坐标约 -1844.51, -756.654, **58.20**）按「Y 在 -800～-550」复用。该点比议会贡品高约 20 码、水平距离约 100 码，不是朗姆酒吧台；且 `spawnDifficulties` 常驻，方案**没有**写 gossip 仅在 event 211 有效。照做后任意周都能喝酒入伙尤朵拉。仓库另有模板 **129009**（Vulpera Mixologist，狐人调酒师），1754 **无刷怪**，方案完全没提。 |
| **Task 3 / Task 10** | 占位/双份：拉米 guid 两套 | F2 预留拉米 **280004003**；主方案 Task 10 写 **280004010**（该号 dump 已占用，见上）；Task 10 又写「可并入 `2026_08_30_10` 或单独 `…_18`」。实施会插两只 133219，或 Task 3 的「creature 插入」与 Task 10 抢同一只。 |
| **Task 5** | 占位符 + 函数名不存在 | 主方案写「`OnSpellCast` / `SpellHit` / 座骑 `IsSummonedBy` **三选一**，F3 写死选哪条」。`CreatureAI` 上的回调是 **`OnSpellCasted`**（`CreatureAI.h`），没有 `OnSpellCast`（那是玩家脚本）。照主方案写 `OnSpellCast` 不会接到 256056。若同时接三条再加 `spell_spawn_parrot`，会双鸟或二次 `ExitVehicle`。F3 已定为：75% `CastSpell(256056)` + 效果 28 防默认召唤。主方案必须删掉「三选一」。 |
| **Task 9** | 函数/路径抽查：SQL 列名不存在 | dump 表是 `creature_template_journal (`**`entry`**, **`JournalEncounterID`**`)`。方案写 `SET journalEncounterId=2093 WHERE creatureId=126848`。按方案原文执行，列名错误，语句失败；实施员若「顺手改列名」也可能改错行。现存行确为 `(126848, 2094)`，改成手册 **2093**、并 `INSERT (126969, 2094)` 的业务方向是对的。 |
| **Task 2** | 与调研矛盾：把内存 `SetData` 写成「落盘」 | 调研 A：友好计数**未写入 instance save**。基类 `GetSaveData` 只写 Headers + 四个 BossState，`WriteSaveDataMore` 默认空。方案目的写「友好进度落盘」，完成定义却只要 `SetData`/`GetData`，没有 `WriteSaveDataMore` / `ReadSaveDataMore`。同一副本会话内掉线可以恢复；**世界服务器重启后**未打完议会的入伙会丢，船长变回敌对。这不是调研要补的那条缺口。 |
| **Task 6** | SQL 抽查：配件表列不完整 | `vehicle_template_accessory.description` 为 **NOT NULL 且无默认值**。方案只写 `(126969, 129448, seat 0)` 与 `(126969, 129359, seat 1)`。按三列 INSERT 会失败。`GetVehicleKit()` 函数本身存在，改 API 的方向是对的。 |
| **调研 D / E 台词** | 调研结论缺对应 Task | 调研 D：乔里 Talk 与 SQL Group 错位、尤朵拉缺进战、劳尔无行。细节 F3 第 6 节写了改法，但**没有**进入任何 Task 的完成定义（Task 7 只动哈兰 126983）。调研 E：Gukguk / Gurgthock `creature_text` 本源未见，Task 6 不补、Task 11 也没写「明确不做」。实施员按主方案走会漏议会与过场台词；后人会把 F3 第 6 节当成已排期。 |
| **调研 I 成就 SQL** | 调研结论缺对应 Task | 调研 I 把地下城成就 SQL 标为数据缺。方案 Task 8 只清脏掉落与议会箱，Task 11 未把成就表列为明确不做。实施员可能自行去改 `achievement` / criteria，越出本阶段「能打出装」。 |

**核对通过（这些项方案没有越界，也没有假装调研已闭合的数字）：**

- **分类 A–K**：十一类都有 Task 或「明确不做」。顺序门、墓园、坐骑、准入 110、LFG、9000000 搬家、129732 战斗体、129600 补刷、钥石/词缀/246779，都写进了 Task 11 / F5。
- **血伤**：Task 1 只删 ×2 / ×1.33，禁止改写成 ×1.25 / ×1.45，禁止把 1754 加进 `InstanceScript.cpp` 团本血量名单，禁止用 `instance_difficulty_multiplier` 1.29 / 1.612 当血量。没有再引入硬乘。
- **掉落**：明确不拆三张 `creature_loot_template`；4776 因本图普通 ItemContext=17 而保留；不改 159633 物品 ID。
- **英雄+**：禁止 `ScriptedAI::IsHeroic()`（只认难度 2）。`freehold.h` **现状没有** `IsFreeholdHeroicPlus`，应当新增：`Map::IsHeroic()` 在难度 8 上也为真（Flags 含 0x01），宏刻意只认 2 与 23、不含钥石 8，与本阶段范围一致。
- **核心越权**：全局铁律禁止改 `Map.cpp` / `Unit.cpp` / `InstanceScript.cpp` 团本名单。各 Task 未要求动这些文件。
- **顺序门**：Task 2 / F5 / Task 11 写清本阶段**不要** override `CheckRequiredBosses`，**不要**改克拉格/议会的 `GetEntry()` 调用点，**不要** `LoadDoorData`。没有「只改 instance 按 0–3 写门」的半截实现。基类约 386 行仍恒 `return true`。
- **占位符字面量**：全文没有 TBD / TODO / 「类似 Task N」。Task 5 的「三选一」本身就是必须删掉的占位（见上表）。
- **抽查存在的符号**：`instance_free_hold::OnCreatureCreate` 第 52–55 行硬乘属实；`captainsDeathCount > 2`、`GetController()` 用 `FindNearestCreature(..., 500.f)`、托萨克 `GetVehicle()`、掷弹兵 `ObjectAccessor::GetCreature`、`ActionStartLaunchBrew` 无外部调用、`AddSC_freehold()` 为空、`NpcSharkBaitBoss = 126841`、`GetDifficultyID()`、`CONDITION_DIFFICULTY_ID = 49`、`ObjectAccessor::GetUnit` 均存在。`IsActiveEvent` API 存在，问题只在 209–211 不是调研钉死的 ID。

**本阶段不做顺序门**：已写清，审查不要求改成做门。哲学五人本最低集合里的「门」在本方案里被明确推迟，并与社区跳尾王刷坐骑的记录对齐；这是范围选择，不是漏写。

**IsFreeholdHeroicPlus**：`freehold.h` 里目前只有阵营/生物/Data 枚举，没有该辅助函数。应当在 Task 2 新增，不能用现成的 `ScriptedAI::IsHeroic()` 代替。

审查结论：**不能按当前正文开写 C++ / SQL。** 至少先改 Task 3 周历（表名未钉死就不能把 209–211 写进 C++ 字面量；guid 必须先 `MAX(guid)+1` 并写进方案）、Task 9 列名、Task 5 切阶段单一路径、Task 10 与 F2 的拉米 guid 合并，再进入实施。

---

## 方案审查 R1 之后的修改

执笔只改文档，未改 C++，未 commit。R1 审查原文整节保留。本轮按 R1 十条与调研「仍开放」约束改 Task 正文和 F2–F5。

该表的核心用途是：对照 R1 每一条说明改了什么、改到哪份文件，避免实施员仍按审查前的 209–211 / 280004xxx / `OnSpellCast` 动手。

| R1 问题 | 怎么改的 |
|---------|----------|
| 1. Task 3 发明 `game_event` 209–211 | 删掉把 `IsActiveEvent(209/210/211)` 当 8.3 真相的写法。周历标**未决**。实施第一步必须在 dump 搜 8.0 自由镇 `game_event` / holiday / worldstate；搜到再用搜到的号并注明出处；搜不到停下来问用户，禁止发明 ID。C++ 只留 `GetActiveFreeholdCrewWeek()` 枚举出口。验收不再写 `.event start 209`。见 Task 3、F2 第 2 节。 |
| 2. 预留 guid 与地图 1864 撞号 | 废弃 280004001–280004010（其中 280004004/007/008/009/010 已是地图 1864）。改为 dump 已核空号的 **280003425–280003430**（1754 最后一行是 280003424）。见 Task 3、F2 第 3 节。 |
| 3. 127124 当喝酒狐人且 gossip 不限当周 | 禁止复用常驻 127124（guid 280003136）。喝酒 NPC 必须当周才刷，gossip 必须再检查 `CrewWeekBilgeRats`。entry 仍未钉死：dump 先搜；搜不到问用户；129009 只作为问用户时的候选，不是已闭合的 8.3 ID。见 F2 第 4 节。 |
| 4. 拉米 guid F2 与主方案不一致 | 全方案唯一 guid **280003427**。INSERT 只出现在 Task 3 的 `2026_08_30_10`；Task 10 只做 `ActionStartLaunchBrew` 接线，禁止第二份 SQL。见 Task 3/10、F2、F3 第 3 节。 |
| 5. Task 5 `OnSpellCast` 不存在；禁止三选一 | 写死：`DamageTaken` 只 `CastSpell(256056)`；下坐骑只在 `CreatureAI::OnSpellCasted`（`CreatureAI.h` 约 263 行）。禁止 `OnSpellCast`、禁止再挂 `OnSuccessfulSpellCast`、禁止 `SpellHit` / `IsSummonedBy` 切阶段。`spell_spawn_parrot` 只挡效果 28 默认召唤。`UnitAI.h` / `ScriptedCreature.h` 无 `OnSpellCasted` 声明，直接 override 基类。见 Task 5、F3 第 1 节。 |
| 6. Task 9 SQL 列名 | 改为 dump 列 `entry` / `JournalEncounterID`。见 Task 9、F4 第 6–7 节。 |
| 7. Task 2 把 `SetData` 写成落盘 | 分类 A 与 Task 2 改为**内存-only**。同一会话 `OnPlayerEnter` 可恢复；卸载/重启后丢失，进游戏验收。本阶段明确不 override `WriteSaveDataMore`。见 Task 2、F5 第 2 节、F2 第 5 节。 |
| 8. Task 6 `description` NOT NULL | INSERT 必须带 `entry, accessory_entry, seat_id, minion, description, summontype, summontimer`。只走 SQL 配件表，不解注释 C++ 召唤。见 Task 6、F3 第 4 节。 |
| 9. 议会与过场台词缺 Task | 议会三船长 `creature_text` 纳入 **Task 4**（`2026_08_30_19`）。Gukguk / Gurgthock 本源未见，**Task 11 明确不做**。见 F3 第 6 节、Task 4/11。 |
| 10. 成就 SQL 缺 Task | **Task 11 明确不做**（`achievement` / criteria；ATT 的 12548 等不挡能打出装）。见 Task 11、F4 第 7 节。 |

`GetVehicleKit`、`OnSpellCasted`、`GetUnit`、`JournalEncounterID` 已在仓库或 dump 核对。guid 280003425–280003430 抽查为空。周历表名与喝酒 NPC entry **仍未闭合**，实施员不得猜 ID。

---

## 方案审查 R2

审查员未写过本方案、未做过 R1 修改、未改 C++。本轮只问：一个没读过对话的工程师能否只凭 Task 正文 + 全局铁律 + 对应细节文档动手，且不必猜。对照仓库 `BFA-HavenCore` 与 dump `DatabasesV1.1/bfa_world/bfa_world.sql`，并核调研「仍开放」是否被正文当成已闭合。

**Status: Issues Found**

该表的核心用途是：标出实施员按当前 Task 正文仍会卡住、选错文件、或写出编不过 / 运行恒空的代码的点。R1 十条已写进 Task 正文的项不重复开单。

| Task 编号 | 问题类型 | 实施员会卡住或写错的原因 |
|----------|----------|--------------------------|
| **Task 3** | 落点未钉死，必须猜文件 | 正文写新脚本「仍写在 `boss_council_o_captains.cpp` **或** `freehold.cpp`」。`AddSC_freehold()` 又必须保持空（Task 11）。实施员若把事件 AI 放进 `freehold.cpp` 且遵守空 `AddSC_freehold`，Murphy / Otis / 指节手 / 喝酒 NPC **永不注册**。应写死只改 `boss_council_o_captains.cpp`，并在 `AddSC_boss_council_o_captains` 列出全部新 `RegisterCreatureAI`（含喝酒 NPC；现在只点名了指节手与 Murphy/Otis）。 |
| **Task 3** | 跨文件接口未写死 | `GetActiveFreeholdCrewWeek()` 与 `NotifyCrewEventComplete()` 写在 `instance_freehold.cpp`，调用方是另一翻译单元的事件 / gossip 脚本。`struct instance_free_hold` **没有**头文件声明。实施员必须猜：在 `freehold.h` 做自由函数声明、把 instance 类公开、还是改成 `SetData` 协议。猜错则 gossip 编不过，或做完事件调不到入伙。 |
| **Task 4** | 按字面改 API 会写错 | `GetController()` 必须返回 `Creature*`。正文写成改成 `instance->GetGuidData(DataCaptainsController)`（返回 `ObjectGuid`，编不过）。同文件 287 行已有正确包装：`ObjectAccessor::GetCreature(*me, instance->GetGuidData(DataCaptainsController))`。实施员若用「看起来更对」的 `InstanceScript::GetCreature(uint32)` 去凑类型：该函数走从未填充的 `_objectGuids`（自由镇只在成员变量里记 GUID，没有 `LoadObjectData`），控制器**恒空**，`>= 2` 永远不会 DONE。必须写死 287 行那种包装，并禁止 `GetCreature(DataCaptainsController)`。 |
| **Task 5** | 完成定义会误伤现有钩子 | 完成定义写「全文件搜不到 `OnSpellCast`」。现有 `OnSpellCastInterrupt`（打断回春酒，约 197 行）会被这条搜索命中。实施员或后续代码审查可能把它删掉或改名。F3 要求钩子写在它旁边并保留。完成定义应改成：禁止声明 `void OnSpellCast(`，保留 `OnSpellCastInterrupt`。 |
| **Task 10** | 两套查找方式，一条走不通 | 接线写成 `FindNearestCreature(NpcRummyMancomb, 80.f)` **或**「instance 记下的 GUID」。instance **没有** `rummyGuid`，`OnCreatureCreate` 也不记 133219；Task 10 的文件清单也不含 `instance_freehold.cpp`。选 GUID 路径的实施员会改到别人的文件，或拿到 `ObjectGuid::Empty`。应写死其中一条，并写清包装函数（不要用 `InstanceScript::GetCreature(uint32)`，理由同 Task 4）。 |

**R1 十条在 Task 正文里的核对（本轮通过，不挡）：**

1. 周历：Task 3 / F2 标未决；搜不到必须停下来问用户；禁止把 209–211 写进 C++ 或 INSERT。验收清单禁止 `.event start 209/210/211`。调研「仍开放」的表名未被假装闭合。  
2. guid：正文与 F2 均为 **280003425–280003430**；dump 无这六行；**禁止** 280004001–280004010。  
3. 喝酒：禁止复用 127124；F2 第 4 节 entry 仍未钉死，搜不到问用户。  
4. 拉米唯一 guid **280003427**；INSERT 只在 Task 3 的 `2026_08_30_10`；Task 10 禁止第二份。  
5. 切阶段单一路径：`OnSpellCasted`（`CreatureAI.h` 第 263 行，仓库**已有**）；禁止 `OnSpellCast`；`IsFreeholdHeroicPlus` 为 Task 2 **新增**，`freehold.h` 现状没有。  
6. Task 9 列名：`entry` / `JournalEncounterID`（dump `CREATE TABLE` 与现存 `(126848, 2094)` 一致）。  
7. Task 2 入伙为内存-only，禁止 `WriteSaveDataMore`。  
8. Task 6 配件 INSERT 含 `description`（该列 `text NOT NULL` 无默认值）。  
9. 议会台词在 Task 4；Gukguk / Gurgthock 在 Task 11 明确不做。  
10. 成就 SQL 在 Task 11 明确不做。

**路径与符号抽查（真实存在，实施可对上）：**

- 家目录七个文件 + `kul_tiras_script_loader.cpp` 已调用 `AddSC_freehold` / `AddSC_instance_freehold`。  
- `instance_free_hold::OnCreatureCreate` 第 52–55 行两条硬乘仍在。  
- `captainsDeathCount > 2`、托萨克 `GetVehicle()`、掷弹兵 `ObjectAccessor::GetCreature`、`ActionStartLaunchBrew` 无外部调用、`AddSC_freehold()` 为空、`CouncilTributePos`、`CONDITION_DIFFICULTY_ID = 49`、`ObjectAccessor::GetUnit`、`GetVehicleKit()`、`GetDifficultyID()`、`DIFFICULTY_HEROIC=2` / `DIFFICULTY_MYTHIC=23`、基类 `CheckRequiredBosses` 在 `InstanceScript.h` 第 386 行恒 `true`。  
- `Spell.cpp` 第 3443 行在 `handle_immediate()` 之后调 `OnSpellCasted`；约 3496 行再调 `OnSuccessfulSpellCast`。

**铁律未被后文推翻。** 正文无 TBD / 「见上文」/「类似 Task N」。周历没有写成确定的 209–211。

**本轮结论：** 不能按当前正文派实施员写 Task 3 / 4 / 5 / 10。至少先写死 Task 3 的 cpp 与跨文件函数声明、Task 4 的 `GetController` 包装、Task 5 完成定义的搜索词、Task 10 的拉米查找单路径，再开写 C++。

---

## 方案审查 R2 之后的修改

执笔只改文档，未改 C++，未 commit。R2 审查原文整节保留。本轮按 R2 五条卡住点把写法写进 **Task 正文**（不是只写在本修改说明里），并同步 F2 / F3。F5 未牵连（入伙仍是内存-only，没有落点/API 二选一）。周历未决规则未改回去：搜不到仍停下来问用户，禁止 209–211。

该表的核心用途是：对照 R2 每一条说明 Task 正文现在写死了什么，实施员不必再猜文件或函数。

| R2 卡住点 | Task 正文怎么写死的 |
|-----------|---------------------|
| 1. Task 3 脚本落点「或」 | **只**放 `boss_council_o_captains.cpp`。`AddSC_freehold()` 保持空，**禁止**把事件 AI 放进 `freehold.cpp`。`AddSC_boss_council_o_captains` 给出完整 `RegisterCreatureAI` 清单（原有四条保留 + `npc_freehold_crew_knuckleduster` / `npc_freehold_murphy` / `npc_freehold_otis` / `npc_freehold_crew_brew`）。喝酒 NPC 即使 entry 未钉死也必须 Register。 |
| 2. Task 3 跨文件接口 | 声明写在 `freehold.h`：`GetActiveFreeholdCrewWeek(InstanceScript const*)` 与 `NotifyCrewEventComplete(InstanceScript*, uint32)`。`instance_free_hold` **不**搬进头文件，**不**改 `InstanceScript.h` 加虚函数。`instance_freehold.cpp` 用 `dynamic_cast<instance_free_hold*>` 包成自由函数。调用点只写 `me->GetInstanceScript()`，不要自己 cast、不要猜 SetData 协议。 |
| 3. Task 4 GetController | 照抄同文件约 287 行：`ObjectAccessor::GetCreature(*me, instance->GetGuidData(DataCaptainsController))`。禁止把 `GetGuidData` 当 `Creature*`。禁止 `InstanceScript::GetCreature(uint32)`（空 `_objectGuids`）。说明了 `InstanceMap*` 与 `InstanceScript*` 两个同名 `instance` 不是同一套 API。 |
| 4. Task 5 完成定义 | 搜索词改为独立钩子 `void OnSpellCast(` / `OnSpellCast(`，禁止搜子串 `OnSpellCast`。必须保留 `OnSpellCastInterrupt`（约 197 行）。钩子仍是 `OnSpellCasted`。 |
| 5. Task 10 拉米查找 | **只**用 `me->FindNearestCreature(uint32(NpcRummyMancomb), 80.f)`。instance 不记 133219，本 Task 不改 `instance_freehold.cpp`，禁止 GUID 路径与 `InstanceScript::GetCreature(uint32)`。 |

仓库核对（本轮抽查，符号存在）：`AddSC_freehold()` 空；`AddSC_boss_council_o_captains` 在 `kul_tiras_script_loader.cpp` 第 84 行；`ObjectAccessor::GetCreature` 在 `ObjectAccessor.h` 第 74 行；`GetController` 现行 `FindNearestCreature(..., 500.f)` 约 373 行；`InstanceScript::GetCreature(uint32)` 在头文件第 256 行 / `InstanceScript.cpp` 第 191–194 行读 `_objectGuids`；`OnSpellCasted` 约 263 行、`OnSpellCastInterrupt` 约 288 行（`CreatureAI.h`）与克拉格约 197 行；`FindNearestCreature` 在 `Object.h` 第 524 行；`OnCreatureCreate` 无 133219 分支；`creature.ScriptName` 按 guid 加载（`ObjectMgr.cpp` 约 2057 行）。

实施员 **不必再猜**：事件 AI 放哪份 cpp、谁 Register、两条跨文件函数写在哪个头、GetController 怎么取 `Creature*`、完成定义怎么搜 `OnSpellCast`、拉米怎么找。仍须问用户的只有调研未闭合项：周历表名/ID、喝酒 NPC entry（搜不到停下来问，禁止 209–211）。

---

## 方案审查 R2 复审

审查员未写过本方案、未做过 R1/R2 修改、未改 C++。本轮只问：一个没读过对话历史的工程师，能否只凭 Task 正文 + 全局铁律 + 对应细节文档动手，且不必猜。对照仓库 `BFA-HavenCore` 与 dump `DatabasesV1.1/bfa_world/bfa_world.sql`，**不采信**文末「方案审查 R2 之后的修改」表，以下每一条都回 Task **正文**核对。

**Status: Approved**

本轮没有「按正文动手会写错文件、编不过、或运行恒空」的卡住项。周历表名/ID 与喝酒 NPC entry 仍是调研未闭合；Task 3 / F2 已写清「搜不到就停下来问用户、禁止把 209–211 当真相」，这不算 Issues Found。

该表的核心用途是：确认上一轮 R2 五条已经写进 Task 正文（不是只出现在修改说明里），实施员不必翻会话。

| 复核项（R2 原卡住点） | Task 正文是否写死 | 仓库/dump 核对 |
|------------------------|-------------------|----------------|
| Task 3 脚本落点无「或」；空 `AddSC_freehold` 则 AI 不能进 `freehold.cpp`；喝酒 NPC 在 Register 清单 | **已写进 Task 3 正文**「脚本落点（写死，禁止「或」）」：只放 `boss_council_o_captains.cpp`；禁止放进 `freehold.cpp`；`AddSC_boss_council_o_captains` 照抄清单含原有四条 + `npc_freehold_crew_knuckleduster` / `npc_freehold_murphy` / `npc_freehold_otis` / `npc_freehold_crew_brew` | `freehold.cpp` 第 75–78 行 `AddSC_freehold()` 为空。现行 `AddSC_boss_council_o_captains`（第 861–874 行）已有那四条旧注册，后接 Spell / AreaTrigger，正文要求 Spell 保持接在 Creature 清单后面。loader 第 84 行已调 `AddSC_boss_council_o_captains()` |
| Task 3 跨文件接口：`freehold.h` 声明 + `instance_freehold.cpp` 自由函数 | **已写进 Task 3 正文**「跨文件接口（照抄能编）」：头文件声明 `GetActiveFreeholdCrewWeek(InstanceScript const*)` 与 `NotifyCrewEventComplete(InstanceScript*, uint32)`；struct **不**搬进头文件、**不**改 `InstanceScript.h`；同一 cpp 用 `dynamic_cast<instance_free_hold*>` 包成自由函数；调用点只写 `me->GetInstanceScript()` | `struct instance_free_hold` 只在 `instance_freehold.cpp` 第 27 行。仓库有 `dynamic_cast` 先例（RTTI 未关）。`boss_council_o_captains.cpp` 已 `#include "freehold.h"`。`freehold.h` 现状没有这两条声明，属本 Task 新增 |
| Task 4 `GetController` 写死 `ObjectAccessor::GetCreature(*me, instance->GetGuidData(...))` | **已写进 Task 4 正文**：完整函数照抄；禁止 `return instance->GetGuidData(...)`；禁止 `instance->GetCreature(DataCaptainsController)` / `GetCreature(uint32(...))` | 现行 `GetController` 第 373–375 行仍是 `FindNearestCreature(..., 500.f)`。`EnterEvadeMode` 第 287 行已是正确包装。`ObjectAccessor.h` 第 74 行签名匹配。`InstanceScript::GetCreature(uint32)` 在头文件第 256 行、`InstanceScript.cpp` 第 191–194 行走 `GetObjectGuid` → `_objectGuids`。自由镇无 `LoadObjectData`；`GetGuidData` 第 179–180 行才返回 `captainsControllerGuid`。`InstanceScript::GetCreature(uint32)` **不会**走该 override，控制器恒空 |
| Task 5 完成定义不会误伤 `OnSpellCastInterrupt` | **已写进 Task 5 禁止事项与完成定义**：禁止搜子串 `OnSpellCast`；搜索词写死为 `void OnSpellCast(` / 作为函数名的 `OnSpellCast(`；必须保留约 197 行 `OnSpellCastInterrupt`；钩子仍是 `OnSpellCasted` | `CreatureAI.h` 第 263 行 `OnSpellCasted`、第 288 行 `OnSpellCastInterrupt`、第 291 行 `OnSuccessfulSpellCast`。仓库**没有** `void OnSpellCast(`。`Spell.cpp` 第 3443 行调 `OnSpellCasted`，第 3496 行再调 `OnSuccessfulSpellCast`。`boss_skycapn_kragg.cpp` 第 197 行已有 `OnSpellCastInterrupt`（打断回春酒）。`OnSpellCast(` 不会命中 `OnSpellCastInterrupt(` 或 `OnSpellCasted(` |
| Task 10 拉米只一条查找路径，且与文件清单一致 | **已写进 Task 10 正文**：英雄+ 只 `FindNearestCreature(uint32(FreeholdCreature::NpcRummyMancomb), 80.f)` 再 `DoAction(ActionStartLaunchBrew)`；禁止 GUID 路径；本 Task **不要**改 `instance_freehold.cpp`、不要 `rummyGuid`；刷怪 INSERT 只在 Task 3 的 `2026_08_30_10`，guid **280003427** | `NpcRummyMancomb = 133219` 在 `freehold.h` 第 44 行。`ActionStartLaunchBrew` 第 88 行，全仓库无外部调用。`OnCreatureCreate` 无 133219 分支。`FindNearestCreature` 在 `Object.h` 第 524 行。模板 ScriptName 已是 `npc_rummy_mancomb`。dump 中 1754 无 `id=133219` 刷怪（guid 133219 是地图 0 的另一只怪） |

**R2 五条均已进入 Task 正文。** 修改说明表与正文一致，但本轮以正文为准。

**路径与符号（本轮重新打开仓库，不是信表）：**

- 家目录七个文件真实存在；`kul_tiras_script_loader.cpp` 第 84–89 行已调用四王、`AddSC_freehold`、`AddSC_instance_freehold`。
- Task 1：`OnCreatureCreate` 第 52–55 行两条独立 `if` 硬乘仍在。`Map::IsHeroic()` 读 Difficulty Flags；35662 `Difficulty.csv` 难度 23 的 Flags=**133=0x85**。`DIFFICULTY_HEROIC=2`、`DIFFICULTY_MYTHIC=23`（`DBCEnums.h`）。`ScriptedAI::IsHeroic()` 只认难度 2。`IsFreeholdHeroicPlus` 现状不在 `freehold.h`，属 Task 2 新增。
- Task 2：`SetHeaders` 在 `InstanceScript.h` 第 501 行。基类 `CheckRequiredBosses` 第 386 行恒 `true`。克拉格 / 议会 `EnterCombat` 仍传 `me->GetEntry()`。`countRaoul` 仍在。
- Task 4：`captainsDeathCount > 2` 在第 175 行。`CouncilTributePos` 第 124 行。`257821` 在 `checkFaction` 第 599–601 行。
- Task 5：`DamageTaken` 第 214–232 行 75% 仍只 `ExitVehicle`，全文件无 256056。`NpcSharkBaitBoss=126841`。dump 126841 的 ScriptName 为空、AIName=SmartAI；129743 的 ScriptName=`npc_sharkbait` 且刷在地图 **603**。`CreatureAISelector.cpp` 第 37–40 行：有 ScriptName 则忽略 SAI。效果 28 = `SPELL_EFFECT_SUMMON`。`UnitAI.h` 第 298 行有 `sGossipHello`。
- Task 6：`ActionThirdEvent` 第 325 行仍 `SummonCreature(NpcTrothak)`。托萨克 `GetVehicle()` 第 787 行。`GetVehicleKit()` 在 `Unit.h` 第 2033 行。dump `vehicle_template_accessory.description` 为 `text NOT NULL`、无默认值；INSERT 列与正文一致。126969 的 VehicleId=**5658**。
- Task 7：掷弹兵第 247 行 `ObjectAccessor::GetCreature`。`GetUnit` / `GetPlayer` 均在 `ObjectAccessor.h`。
- Task 9：dump `creature_template_journal (`**`entry`**, **`JournalEncounterID`**`)`；现存 `(126848, 2094)`、`(126832, 2102)`、`(126983, 2095)`，无托萨克行。`instance_encounters`：`(2093, 0, 126832)`、`(2094, 0, 126848)`、`(2095, 0, 126969)`、`(2096, 0, 126983)`。
- `creature.ScriptName`：dump `CREATE TABLE creature` 有该列；`ObjectMgr.cpp` 第 2057–2058、2131 行按 guid 加载；`Creature::GetScriptId` 第 2948–2954 行空则回退模板。
- `CONDITION_DIFFICULTY_ID = 49`（`ConditionMgr.h` 第 105 行）。

**ID 与调研定稿 / 细节文档：没有被方案改歪。** guid **280003425–280003430** 在 dump 的 `creature` 全文搜不到（1754 最后一行确为 **280003424**）；拉米唯一 guid **280003427** 与 F2 / Task 3 / Task 10 一致。126841、256056、288636、257821、`IsFreeholdHeroicPlus` 只认 2|23、手册/战斗遭遇两套 2093–2096 与 2102/2093/2094/2095，与调研 R3 一致。模板 `HealthModifier`：克拉格 35、乔里/劳尔 22、托萨克 36、哈兰 50。常驻 127124 为 guid **280003136**（-1844.51, -756.654, 58.1959），正文禁止复用。Otis 用 **129441**，禁止 12944。

**铁律未被后文推翻。** Task 正文无 TBD / 「见上文」/「类似 Task N」。Task 3 的「见下『跨文件接口』」与「见 F2」指向同一份方案/细节，不是翻会话。Task 3/4/10 建议串行同一 cpp，边界写清：拉米 INSERT 只在 Task 3，Task 10 只接线。

**周历仍须是未决。** 调研「真正还没钉死」仍是服务端用哪张表。Task 3 / F2 / 验收清单均禁止 `IsActiveEvent(209/210/211)` 与 `.event start 209/210/211`。搜不到则成员函数只返回 `CrewWeekNone` 并停问用户。喝酒 NPC entry 同样未钉死，struct 仍须 Register，SQL 等用户确认。这两项不是 Issues Found。

**不升为 Issues Found 的残余（实施时按 dump 邻近行抄 INSERT 即可，不是发明 ID）：** Task 3 的 SQL 照抄块只写了 `ScriptName` 的 UPDATE，完整 `creature` INSERT 列以 dump 邻近 1754 行为准（含 `zoneId`/`areaId`/`ScriptName`）。Murphy / Otis / 喝酒 NPC 走 `sGossipHello`，`npcflag` 默认 0，INSERT 时需要打开 gossip 旗，这是 Trinity 常规，正文没有另给一个自造 ID。

**本轮结论：** 可以派实施员按 Task 1→2→9→8→5→3→4→10→6→7→11 写 C++ / SQL。本审查轮次只出方案审查，不写 C++。实施员在周历 ID 与喝酒 NPC entry 上必须停问，不得填 209–211 或擅自选定 entry。
