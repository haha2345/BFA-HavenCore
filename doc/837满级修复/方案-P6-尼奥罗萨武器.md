# P6 尼奥罗萨武器写死效果 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让尼奥罗萨六把武器的写死腐蚀效果在 8.3.7.35662 上按正式服行为可玩，并且每条都有独立的 HavenLab（腐蚀效果测试台）第 1 层打桩包。

**第 1 层状态（2026-08-29）：进游戏验收通过。**

**Architecture:** 不新建管理器。穿脱继续走 `Player::ApplyItemEquipSpell`。战斗行为全部加在 `spell_corruption.cpp`，一条效果一套 AuraScript/SpellScript，SQL 绑 `spell_script_names`；**仅当本核 `ProcFlags` 读出来是 0**（丢掉了 `ProcTypeMask[1]`）才允许额外插 `spell_proc`，形状抄虚空回响。数字只从 35662 `SpellInfo` 读；DBC 表达不了的 fallback 必须带一次性 ERROR，并登记 [设计-P6-尼奥罗萨武器.md](设计-P6-尼奥罗萨武器.md) 第 7 节。

核验修订（[吞噬与灼热细节核验](7ba38e15-da29-4e6f-aab7-2582b5f19ee0)、[虚空鞭与黑曜石核验](52eb763c-5a71-46f2-9130-e289bd3dfe07)、[第二遍打脸六条武器](3641eeb2-446b-4da4-afe7-dc44fdc49774)）：吞噬用 `SetEffectValue`；灼热必须 `spell_proc` + 60 度锥、禁止 `FilterTargets`；虚空之鞭施放 **317291**（9 码/60 度）+ **319241** 减速，掩码跟 35662 的 4（仅白字），不要跟 Icy Veins「Your attacks」；黑曜石爆炸是 **316661**、护甲光环零脚本、脱战留层、6 目标分摊只给这一条。

**Tech Stack:** C++17，TrinityCore 8.3.7 脚本 API（`RegisterAuraScript` / `RegisterSpellScript`），世界库 `spell_script_names`，HavenLab Lua pack。仓库没有单元测试框架；每个任务的自动化替代品是「先写 pack 期望，再写脚本，再编译 `worldserver`」。

## Global Constraints

- 对照版本：8.3.7.35662 最终版（含热修）。现役 Wowhead BasePoints 作废。
- 禁止 `if (corruption >= N)`。禁止写死 2/5/25/30/700/2000，除非第 7 节 fallback 且打 ERROR。
- **默认禁止** `spell_proc` 覆盖 DBC。**唯一例外**：`SpellInfo::ProcFlags == 0` 是因为本核只读 `ProcTypeMask[0]`、丢掉了 `[1]`（灼热 316698）。此时插表复原 mask0=69904（与虚空回响同形状），`SpellTypeMask` 只开伤害，`Cooldown=100`。禁止抄 SimC 的治疗/平砍掩码。
- 学校伤害用 `SetHitDamage` / `SPELLVALUE_BASE_POINT0`。吸取生命（316617）用 `OnEffectHitTarget` + `SetEffectValue`，让 `EffectHealthLeech` 自己伤和疗。禁止再叠 `DealDamage` / `DealHeal`。
- 施法旗复用已有 `InfiniteStarsCastFlags()`（忽略公共冷却、当前施法、消耗；不含 `IGNORE_TARGET_CHECK`，除非这一下是自我圆形打自己人）。
- ID 进 `enum`，施法处禁止裸数字。
- 路径相对仓库根。一条效果一个提交，主题 `core/spells: implement <effect> Ny'alotha weapon`。
- 第 1 层用 `.lab test` / `.aura` 驱动，不穿真武器。会拉未进战的怪写进测试步骤。
- 改 `addons/HavenLab/` 必须同步客户端 AddOns 镜像。
- `.lab test` 必须同时改：`FindLabTest` 的 `tests[]`、三处 Usage 字符串、**`RemoveHavenLabCorruptionAuras` 的 passives[]**（漏清 316744 / 316703 / 317420 会污染 A1–P5）。
- 每个任务隐含本节全部约束。

---

## 文件总表

| 路径 | 职责 |
|------|------|
| `src/server/scripts/Spells/spell_corruption.cpp` | 六条武器的 enum、求值函数、脚本、启动转储、Glimpse 排除表 |
| `src/server/scripts/Commands/cs_misc.cpp` | `.lab test` 六行 key、三处 Usage、`RemoveHavenLabCorruptionAuras` 的 passives[] |
| `sql/updates/world/2026_08_29_04_world_corruption_devour_vitality.sql` 起 | Task 0 无 SQL。今日已有 `_00`–`_03`，P6 从 `_04` 起。灼热那条额外插 `spell_proc` |
| `addons/HavenLab/HavenLab_Packs.lua` | 六张 pack |
| `doc/837满级修复/设计-P6-尼奥罗萨武器.md` | 事实与未决；转储后回填 EffectIndex |
| `doc/837满级修复/腐蚀特效清单.md` | Haven 列 |
| `doc/837满级修复/设计-腐蚀-Blizzlike.md` | 解除边界 + 热修表 |
| `doc/837满级修复/方案-腐蚀-剩余修复.md` | P6 从「边界外」改指向本文 |

不改 `spell_script_loader.cpp`（`AddSC_corruption_spell_scripts` 已在）。不改 `UpdateCorruption()`。不改 `Item.cpp`。

---

### Task 0: 启动转储 + 解除文档边界

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`（`AddSC_corruption_spell_scripts` 末尾）
- Modify: `doc/837满级修复/设计-腐蚀-Blizzlike.md`（边界表）
- Modify: `doc/837满级修复/方案-腐蚀-剩余修复.md`（P6 行）

**Interfaces:**
- Consumes: 已有 `void LogCorruptionSpellInfo(char const* tag, uint32 id);`
- Produces: 世界服日志里的 `NyalothaWeapons.dump` 行；后续任务按转储填写 EffectIndex

- [ ] **Step 1: 在 `AddSC_corruption_spell_scripts` 末尾加转储（数字可用字面量，因为 enum 还没加；下一任务会改成 enum）**

在 `ResolveDelusionChain();` 之后增加：

```cpp
    static uint32 const nyalothaWeaponDump[] = {
        318294, 316615, 316617,
        318293, 316698, 316703, 316704,
        316780, 316782,
        317290, 317291, 319241,
        318299, 316717, 316744,
        316651, 317420, 316661
    };
    for (uint32 id : nyalothaWeaponDump)
        LogCorruptionSpellInfo("NyalothaWeapons.dump", id);
```

- [ ] **Step 2: 解除总纲边界**

`设计-腐蚀-Blizzlike.md`「边界」里删除「尼奥罗萨武器固定效果（吞噬活力等）」这一行，改成：

```
- 尼奥罗萨武器写死效果：见 设计-P6-尼奥罗萨武器.md（P6 已立项）
```

`方案-腐蚀-剩余修复.md` P6 行改成：「已立项，方案见 方案-P6-尼奥罗萨武器.md。本总排期仍一次一条。」

- [ ] **Step 3: 编译并读转储**

```powershell
cmake --build build --config RelWithDebInfo --target worldserver
```

启动世界服一次（或只跑到脚本注册）。在日志里搜索 `NyalothaWeapons.dump`。把每条技能的 `effect` / `aura` / `amount` / `trigger` / `targetA` / `targetB` / `procFlags` / `ppm` / `coeff` 抄进 `设计-P6-尼奥罗萨武器.md` 第 10 节。

预期：18 个 ID 都有行。缺行是 ERROR，停下来查 DB2，不要猜。`317292` 不要加进数组。

- [ ] **Step 4: 按转储改后续任务将用的 EffectIndex（只改设计文档，不写战斗脚本）**

必须能回答：

- 318294 是否 LINKED 316615
- 316617 是否 `SPELL_EFFECT_HEALTH_LEECH`（本核值 9）
- 316704 的 TargetA/B 是不是锥形
- 317290 伤害在哪个 Effect、是不是 AoE
- 316651 Dummy3 是 700 还是 300
- 317420 周期是否 1000ms、最大层是否 30
- 316780 Dummy1 是否 2000、是否 Trigger 316782

- [ ] **Step 5: 提交（等人明确说提交再做）**

```
docs: lift P6 Ny'alotha weapon boundary and dump 35662 spell rows
```

---

### Task 1: 共享 enum、排除表、`.lab test` 六个空位

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`
- Modify: `src/server/scripts/Commands/cs_misc.cpp`

**Interfaces:**
- Consumes: Task 0 转储（决定哪些 ID 进 enum）
- Produces: 下列 enum 与 fallback；后续任务只引用这些名字

在现有 Glimpse enum **后面**（不要塞进 Glimpse 枚举）新增独立枚举：

```cpp
enum DevourVitalitySpells
{
    SPELL_DEVOUR_VITALITY_ITEM = 318294,
    SPELL_DEVOUR_VITALITY_PROC = 316615,
    SPELL_DEVOUR_VITALITY_LEECH = 316617
};

enum SearingFlamesSpells
{
    SPELL_SEARING_FLAMES_ITEM  = 318293,
    SPELL_SEARING_FLAMES_PROC  = 316698,
    SPELL_SEARING_FLAMES_BUFF  = 316703,
    SPELL_SEARING_FLAMES_BREATH = 316704
};

enum WhisperedTruthsSpells
{
    SPELL_WHISPERED_TRUTHS_PROC = 316780,
    SPELL_WHISPERED_TRUTHS_LOG  = 316782
};

enum FlashOfInsightSpells
{
    // 318299 / 316717 already exist on GlimpseOfClaritySpells as
    // SPELL_FLASH_OF_INSIGHT_ITEM / SPELL_FLASH_OF_INSIGHT_PROC.
    SPELL_FLASH_OF_INSIGHT_BUFF = 316744
};

enum LashOfTheVoidSpells
{
    SPELL_LASH_OF_THE_VOID        = 317290,
    SPELL_LASH_OF_THE_VOID_DAMAGE = 317291,
    SPELL_LASH_OF_THE_VOID_SLOW   = 319241
};

enum ObsidianSkinSpells
{
    SPELL_OBSIDIAN_SKIN           = 316651,
    SPELL_OBSIDIAN_DESTRUCTION    = 317420,
    SPELL_OBSIDIAN_DESTRUCTION_DAMAGE = 316661
};

constexpr int32 DEVOUR_VITALITY_PCT_FALLBACK = 2;
constexpr int32 SEARING_BREATH_PCT_FALLBACK = 5;
constexpr uint32 SEARING_FLAMES_MAX_STACKS_FALLBACK = 30;
constexpr int32 WHISPERED_TRUTHS_TRIM_MS_FALLBACK = 2000;
constexpr uint32 FLASH_OF_INSIGHT_MAX_STACKS_FALLBACK = 8;
constexpr int32 OBSIDIAN_ARMOR_PCT_FALLBACK = 700;
constexpr float OBSIDIAN_SPLIT_PER_TARGET = 0.15f;
constexpr uint32 OBSIDIAN_SPLIT_CAP = 6;
```

若 Task 0 证明 316698 不存在、proc 就在 318293 上：把 `SPELL_SEARING_FLAMES_PROC` 改成 318293，并在设计文档记一笔。不要同时绑两个 proc。

- [ ] **Step 1: 把上列 enum / constexpr 加进 `spell_corruption.cpp`**

- [ ] **Step 2: 扩大 `IsGlimpseExcludedSpell`**

在现有 `switch` 里增加全部武器战斗 ID（318294, 316615, 316617, 318293, 316698, 316703, 316704, 316780, 316782, 317290, 317291, 319241, 316744, 316651, 317420, 316661）。318299 / 316717 已在，不要重复。不要加 317292。

- [ ] **Step 3: 把 Task 0 的转储数组改成 enum 名字**

- [ ] **Step 4: `.lab test` 六行**

`cs_misc.cpp` 的 `tests[]` 追加（`addAura` 用转储认定的**隐藏 proc**，不是物品外壳）：

```cpp
            { "devour",    316615, "labdevour",    "devour vitality",  "Other corruption test auras removed." },
            { "searing",   316698, "labsearing",   "searing flames",   "Other corruption test auras removed." },
            { "whisper",   316780, "labwhisper",   "whispered truths", "Other corruption test auras removed." },
            { "flash",     316717, "labflash",     "flash of insight", "Other corruption test auras removed." },
            { "lash",      317290, "lablash",      "lash of the void", "Other corruption test auras removed." },
            { "obsidian",  316651, "labobsidian",  "obsidian skin",    "Other corruption test auras removed." },
```

同步改两处 Usage / unknown key 字符串，把六个 key 接在 `consequences` 后面。

- [ ] **Step 4b: `RemoveHavenLabCorruptionAuras` 的 `passives[]` 追加**

漏清会污染后续 `.lab test`。至少加上：318294, 316615, 318293, 316698, 316703, 316780, 318299, 316717, 316744, 317290, 316651, 317420。选中目标再清 319241（减速）。

- [ ] **Step 5: 编译 `worldserver`**

预期：链接成功。还没有 `RegisterAuraScript`，进游戏 `.lab test devour` 只会挂光环、没有黄字。

- [ ] **Step 6: 提交（等人说再做）**

```
core/spells: add Ny'alotha weapon spell ids and lab keys
```

---

### Task 2: 吞噬活力

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`
- Create: `sql/updates/world/2026_08_29_04_world_corruption_devour_vitality.sql`
- Modify: `addons/HavenLab/HavenLab_Packs.lua`（及客户端镜像）

**Interfaces:**
- Consumes: `SPELL_DEVOUR_VITALITY_*`、`LabNotify`、`InfiniteStarsCastFlags`、`SumCorruptionRankDummy` 的读 Dummy 形状
- Produces: `int32 DevourVitalityHealthPct(Unit const*)`；`spell_devour_vitality_proc` / `spell_devour_vitality_leech`

- [ ] **Step 1: 先写 pack 期望（失败态：没有 DEVOUR_PROC）**

在 `HavenLab_Packs.lua` 的 `HL.PACKS` 增加：

```lua
    devour = {
        key = "devour",
        title = "吞噬活力",
        order = 30,
        serverCmd = ".lab test devour",
        hint = "只用平砍。技能打桩不应出 DEVOUR_PROC。",
        startText = "【测试开始】已挂 316615。对木桩平砍。看 DEVOUR_PROC 和 316617。",
        startPrint = "已挂 316615。平砍木桩。",
        ids = { 318294, 316615, 316617 },
        labels = {
            [318294] = "物品驱动",
            [316615] = "隐藏proc",
            [316617] = "吸血伤害",
        },
        chain = {
            { id = 316615, role = "隐藏proc", want = "aura-self", hidden = true,
              hintFail = "没挂上 316615。" },
            { id = 316617, role = "吸血", want = "labmsg", labType = "DEVOUR_PROC",
              procStep = true,
              hintFail = "平砍没有 DEVOUR_PROC。技能触发了反而说明掩码过宽。" },
        },
    },
```

- [ ] **Step 2: 求值函数 + 脚本**

```cpp
int32 DevourVitalityHealthPct(Unit const* owner)
{
    uint32 const ranks[] = { SPELL_DEVOUR_VITALITY_ITEM, SPELL_DEVOUR_VITALITY_PROC };
    int32 pct = SumCorruptionRankDummy(owner, ranks, 2, EFFECT_0, true,
        DEVOUR_VITALITY_PCT_FALLBACK, "DevourVitality");
    if (pct < 1)
        pct = SumCorruptionRankDummy(owner, ranks, 2, EFFECT_1, true,
            DEVOUR_VITALITY_PCT_FALLBACK, "DevourVitality");
    return pct < 1 ? DEVOUR_VITALITY_PCT_FALLBACK : pct;
}

void CastDevourVitality(Unit* caster, Unit* target)
{
    if (!caster || !target || !caster->IsAlive() || !target->IsAlive() || target == caster)
        return;
    if (!caster->_IsValidAttackTarget(target, sSpellMgr->GetSpellInfo(SPELL_DEVOUR_VITALITY_LEECH)))
        return;
    bool ok = caster->CastSpell(target, SPELL_DEVOUR_VITALITY_LEECH, InfiniteStarsCastFlags());
    LabNotify(caster, "DEVOUR_PROC", Trinity::StringFormat(
        "target=%s pct=%d ok=%u", target->GetName().c_str(),
        DevourVitalityHealthPct(caster), ok ? 1u : 0u));
}

class spell_devour_vitality_proc : public AuraScript
{
    PrepareAuraScript(spell_devour_vitality_proc);

    bool Validate(SpellInfo const* /*spellInfo*/) override
    {
        return ValidateSpellInfo({ SPELL_DEVOUR_VITALITY_LEECH, SPELL_DEVOUR_VITALITY_ITEM });
    }

    bool CheckProc(ProcEventInfo& eventInfo)
    {
        if (!(eventInfo.GetTypeMask() & PROC_FLAG_DONE_MELEE_AUTO_ATTACK))
            return false;
        return eventInfo.GetActionTarget() != nullptr;
    }

    void HandleProc(ProcEventInfo& eventInfo)
    {
        PreventDefaultAction();
        if (Unit* caster = GetTarget())
            CastDevourVitality(caster, eventInfo.GetActionTarget());
    }

    void Register() override
    {
        DoCheckProc += AuraCheckProcFn(spell_devour_vitality_proc::CheckProc);
        OnProc += AuraProcFn(spell_devour_vitality_proc::HandleProc);
    }
};

class spell_devour_vitality_leech : public SpellScript
{
    PrepareSpellScript(spell_devour_vitality_leech);

    void HandleLeech(SpellEffIndex /*effIndex*/)
    {
        Unit* caster = GetCaster();
        if (!caster)
            return;
        int32 pct = DevourVitalityHealthPct(caster);
        int64 value = int64(caster->GetMaxHealth()) * pct / 100;
        if (value < 1)
            value = 1;
        // EffectHealthLeech reads the effect-local damage, not GetHitDamage().
        SetEffectValue(int32(value));
    }

    void Register() override
    {
        OnEffectHitTarget += SpellEffectFn(spell_devour_vitality_leech::HandleLeech,
            EFFECT_0, SPELL_EFFECT_HEALTH_LEECH);
    }
};
```

`AddSC` 增加：

```cpp
    RegisterAuraScript(spell_devour_vitality_proc);
    RegisterSpellScript(spell_devour_vitality_leech);
```

若 Task 0 显示 316615 的 ProcFlags **已经只有**近战自动攻击：删掉 `CheckProc` 里的掩码判断，只留目标非空。不要双重过滤把几率打没。

若 316617 的 HEALTH_LEECH 已经带非 0 BP 且等于 maxHP×2%：`HandleLeech` 不要改数值。

- [ ] **Step 3: SQL**

```sql
-- 837 blizzlike: Devour Vitality (An'zig Vra). 316617 is HEALTH_LEECH;
-- script fills SetEffectValue from caster max HP * Dummy. Do NOT DealHeal.
-- 316615 mask0 already has melee auto-attack; do NOT write spell_proc.

DELETE FROM `spell_script_names` WHERE `spell_id` IN (316615, 316617);
INSERT INTO `spell_script_names` (`spell_id`, `ScriptName`) VALUES
(316615, 'spell_devour_vitality_proc'),
(316617, 'spell_devour_vitality_leech');
```

- [ ] **Step 4: 编译 `worldserver`**

- [ ] **Step 5: 进游戏第 1 层（人做）**

`.lab test devour`，平砍团本桩。预期：间歇出现 `DEVOUR_PROC`；CLEU 316617 暗影伤约等于自己最大生命的 2%；自己回血（同一条吸取生命，不是第二条治疗）；猛击不应出。未进游戏不得把清单 Haven 列改成「通过」。

- [ ] **Step 6: 提交（等人说再做）**

```
core/spells: implement Devour Vitality Ny'alotha weapon
```

---

### Task 3: 灵光一闪（法器 Flash of Insight）

放在灼热前面：形状接近已有 buff 脚本，且必须先和 Glimpse 分家。

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`
- Create: `sql/updates/world/2026_08_29_05_world_corruption_flash_of_insight.sql`
- Modify: `addons/HavenLab/HavenLab_Packs.lua`

**Interfaces:**
- Consumes: `SPELL_FLASH_OF_INSIGHT_ITEM` / `PROC`（已有）、`SPELL_FLASH_OF_INSIGHT_BUFF`
- Produces: `int32 FlashOfInsightPctPerStack(Unit const*)`、`uint32 FlashOfInsightMaxStacks()`、`void RerollFlashOfInsight(Unit*)`

- [ ] **Step 1: pack**

```lua
    flash = {
        key = "flash",
        title = "灵光一闪（法器）",
        order = 31,
        serverCmd = ".lab test flash",
        hint = "挂上就该有 316744。施法后层数重滚 1–8，不是 +1。不要和 Glimpse 减 CD 搞混。",
        startText = "【测试开始】已挂 316717。看 316744 层数和面板智力。",
        startPrint = "已挂 316717。身上应立刻有 316744。",
        ids = { 318299, 316717, 316744, 315573 },
        labels = {
            [318299] = "物品驱动",
            [316717] = "隐藏proc",
            [316744] = "智力层",
            [315573] = "随机腐蚀Glimpse（不应出现）",
        },
        chain = {
            { id = 316717, role = "隐藏proc", want = "aura-self", hidden = true,
              hintFail = "没挂 316717。" },
            { id = 316744, role = "智力层", want = "labmsg", labType = "FLASH_ROLL",
              procStep = true,
              hintFail = "没有 FLASH_ROLL。挂上时就该立刻滚一层。" },
        },
    },
```

- [ ] **Step 2: 脚本**

```cpp
int32 FlashOfInsightPctPerStack(Unit const* owner)
{
    uint32 const ranks[] = { SPELL_FLASH_OF_INSIGHT_ITEM };
    int32 pct = SumCorruptionRankDummy(owner, ranks, 1, EFFECT_0, true, 1, "FlashOfInsight");
    return pct < 1 ? 1 : pct;
}

uint32 FlashOfInsightMaxStacks()
{
    if (SpellInfo const* buff = sSpellMgr->GetSpellInfo(SPELL_FLASH_OF_INSIGHT_BUFF))
        if (buff->StackAmount >= 1)
            return buff->StackAmount;
    static bool logged = false;
    if (!logged)
    {
        logged = true;
        TC_LOG_ERROR("scripts", "FlashOfInsight: StackAmount missing, using %u",
            FLASH_OF_INSIGHT_MAX_STACKS_FALLBACK);
    }
    return FLASH_OF_INSIGHT_MAX_STACKS_FALLBACK;
}

void RerollFlashOfInsight(Unit* owner)
{
    if (!owner || !owner->IsAlive())
        return;
    uint32 maxStacks = FlashOfInsightMaxStacks();
    uint32 stacks = urand(1, maxStacks);
    if (!owner->HasAura(SPELL_FLASH_OF_INSIGHT_BUFF))
        owner->CastSpell(owner, SPELL_FLASH_OF_INSIGHT_BUFF, InfiniteStarsCastFlags());
    if (Aura* aura = owner->GetAura(SPELL_FLASH_OF_INSIGHT_BUFF))
        aura->SetStackAmount(int32(stacks));
    LabNotify(owner, "FLASH_ROLL", Trinity::StringFormat(
        "stacks=%u max=%u pct=%d", stacks, maxStacks, FlashOfInsightPctPerStack(owner)));
}

class spell_flash_of_insight_proc : public AuraScript
{
    PrepareAuraScript(spell_flash_of_insight_proc);

    bool Validate(SpellInfo const* /*spellInfo*/) override
    {
        return ValidateSpellInfo({ SPELL_FLASH_OF_INSIGHT_BUFF, SPELL_FLASH_OF_INSIGHT_ITEM });
    }

    void HandleApply(AuraEffect const* /*aurEff*/, AuraEffectHandleModes /*mode*/)
    {
        RerollFlashOfInsight(GetTarget());
    }

    void HandleRemove(AuraEffect const* /*aurEff*/, AuraEffectHandleModes /*mode*/)
    {
        if (Unit* owner = GetTarget())
            owner->RemoveAurasDueToSpell(SPELL_FLASH_OF_INSIGHT_BUFF);
    }

    void HandleProc(ProcEventInfo& /*eventInfo*/)
    {
        PreventDefaultAction();
        RerollFlashOfInsight(GetTarget());
    }

    void Register() override
    {
        AfterEffectApply += AuraEffectApplyFn(spell_flash_of_insight_proc::HandleApply,
            EFFECT_0, SPELL_AURA_ANY, AURA_EFFECT_HANDLE_REAL);
        AfterEffectRemove += AuraEffectRemoveFn(spell_flash_of_insight_proc::HandleRemove,
            EFFECT_0, SPELL_AURA_ANY, AURA_EFFECT_HANDLE_REAL);
        OnProc += AuraProcFn(spell_flash_of_insight_proc::HandleProc);
    }
};

class spell_flash_of_insight_buff : public AuraScript
{
    PrepareAuraScript(spell_flash_of_insight_buff);

    void CalculateAmount(AuraEffect const* /*aurEff*/, int32& amount, bool& canBeRecalculated)
    {
        canBeRecalculated = true;
        amount = FlashOfInsightPctPerStack(GetUnitOwner());
    }

    void Register() override
    {
        // Aura name must match the 35662 dump. If it is not MOD_PERCENT_STAT,
        // change the hook to the dumped aura; do not invent a second modifier.
        DoEffectCalcAmount += AuraEffectCalcAmountFn(spell_flash_of_insight_buff::CalculateAmount,
            EFFECT_0, SPELL_AURA_MOD_PERCENT_STAT);
    }
};
```

若转储 aura 不是 `SPELL_AURA_MOD_PERCENT_STAT`：只改 `Register` 里的 aura 枚举，不要改重滚逻辑。

`AddSC`：`RegisterAuraScript(spell_flash_of_insight_proc);` `RegisterAuraScript(spell_flash_of_insight_buff);`

- [ ] **Step 3: SQL**

```sql
-- 837 blizzlike: Flash of Insight (Mar'kowa). Reroll 316744 to 1..N stacks.
-- Not Glimpse of Clarity (315573). Do NOT write spell_proc.

DELETE FROM `spell_script_names` WHERE `spell_id` IN (316717, 316744);
INSERT INTO `spell_script_names` (`spell_id`, `ScriptName`) VALUES
(316717, 'spell_flash_of_insight_proc'),
(316744, 'spell_flash_of_insight_buff');
```

若 318294 那种 LINKED 在这里也成立（318299 LINKED → 316717），不要把 318299 绑进 `spell_corruption_rank_driver`，除非转储证明 318299 自己没有 LINKED、不挂 316717。那时再加成单元素家族，单独一条 SQL。

- [ ] **Step 4: 编译**

- [ ] **Step 5: 进游戏**

智力职业 `.lab test flash`。预期：立刻 `FLASH_ROLL`；层 1–8；施法后层跳到另一个 1–8；**没有** 315573；转盘不会无故 −3 秒。

- [ ] **Step 6: 提交**

```
core/spells: implement Flash of Insight Ny'alotha weapon
```

---

### Task 4: 低语真相

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`
- Create: `sql/updates/world/2026_08_29_06_world_corruption_whispered_truths.sql`
- Modify: `addons/HavenLab/HavenLab_Packs.lua`

**Interfaces:**
- Consumes: `SpellHistory::GetRemainingCooldown` / `ModifyCooldown`（须臾洞察已用）、`TryGlimpseTrim` 的职业技能口径
- Produces: `int32 WhisperedTruthsTrimMs(Unit const*)`、`void CastWhisperedTruths(Player*)`

- [ ] **Step 1: pack**

```lua
    whisper = {
        key = "whisper",
        title = "低语真相",
        order = 32,
        serverCmd = ".lab test whisper",
        hint = "猎人自动射击。先开一个职业技能让转盘转起来。",
        startText = "【测试开始】已挂 316780。自动射击，看 WHISPER_PROC。",
        startPrint = "已挂 316780。用自动射击。非猎人不应减 CD。",
        ids = { 316780, 316782 },
        labels = {
            [316780] = "隐藏proc",
            [316782] = "战斗记录",
        },
        chain = {
            { id = 316780, role = "隐藏proc", want = "aura-self", hidden = true,
              hintFail = "没挂 316780。" },
            { id = 316782, role = "减CD", want = "labmsg", labType = "WHISPER_PROC",
              procStep = true,
              hintFail = "自动射击没有 WHISPER_PROC。先让一个猎人技能在冷却中。" },
        },
    },
```

- [ ] **Step 2: 脚本**

```cpp
int32 WhisperedTruthsTrimMs(Unit const* owner)
{
    if (SpellInfo const* proc = sSpellMgr->GetSpellInfo(SPELL_WHISPERED_TRUTHS_PROC))
        if (SpellEffectInfo const* effect = proc->GetEffect(EFFECT_0))
        {
            int32 ms = effect->BasePoints;
            if (ms >= 100)
                return ms;
            if (ms >= 1 && ms <= 10)
                return ms * IN_MILLISECONDS;
        }
    static bool logged = false;
    if (!logged)
    {
        logged = true;
        TC_LOG_ERROR("scripts", "WhisperedTruths: Dummy missing, using %d ms",
            WHISPERED_TRUTHS_TRIM_MS_FALLBACK);
    }
    return WHISPERED_TRUTHS_TRIM_MS_FALLBACK;
}

bool IsWhisperedTruthsCandidate(Player const* player, SpellInfo const* info)
{
    if (!player || !info || info->IsPassive())
        return false;
    if (!info->SpellFamilyName)
        return false;
    if (!info->GetRecoveryTime() && !info->ChargeCategoryId)
        return false;
    if (IsGlimpseExcludedSpell(info->Id))
        return false;
    return true;
}

void CastWhisperedTruths(Player* player)
{
    if (!player)
        return;

    SpellHistory* history = player->GetSpellHistory();
    if (!history)
        return;

    int32 trim = WhisperedTruthsTrimMs(player);
    std::vector<uint32> down;
    for (PlayerSpellMap::value_type const& kv : player->GetSpellMap())
    {
        if (!kv.second || kv.second->state == PLAYERSPELL_REMOVED)
            continue;
        SpellInfo const* info = sSpellMgr->GetSpellInfo(kv.first);
        if (!IsWhisperedTruthsCandidate(player, info))
            continue;
        if (!history->GetRemainingCooldown(info))
            continue;
        down.push_back(info->Id);
    }
    if (down.empty())
    {
        LabNotify(player, "WHISPER_PROC", "spell=0 trimmed=0 skipped=none_down");
        return;
    }

    uint32 id = down[urand(0, uint32(down.size() - 1))];
    history->ModifyCooldown(id, -trim);
    if (SpellInfo const* trimmed = sSpellMgr->GetSpellInfo(id))
        if (trimmed->ChargeCategoryId)
            history->ReduceChargeCooldown(trimmed->ChargeCategoryId, uint32(trim));
    if (sSpellMgr->GetSpellInfo(SPELL_WHISPERED_TRUTHS_LOG))
        player->CastSpell(player, SPELL_WHISPERED_TRUTHS_LOG, InfiniteStarsCastFlags());
    LabNotify(player, "WHISPER_PROC", Trinity::StringFormat(
        "spell=%u trimmed=%d pool=%u", id, trim, uint32(down.size())));
}

class spell_whispered_truths : public AuraScript
{
    PrepareAuraScript(spell_whispered_truths);

    bool CheckProc(ProcEventInfo& eventInfo)
    {
        return (eventInfo.GetTypeMask() & PROC_FLAG_DONE_RANGED_AUTO_ATTACK) != 0;
    }

    void HandleProc(ProcEventInfo& /*eventInfo*/)
    {
        PreventDefaultAction();
        if (Player* player = GetTarget()->ToPlayer())
            CastWhisperedTruths(player);
    }

    void Register() override
    {
        DoCheckProc += AuraCheckProcFn(spell_whispered_truths::CheckProc);
        OnProc += AuraProcFn(spell_whispered_truths::HandleProc);
    }
};
```

若转储 ProcFlags 已经只有自动射击：去掉 `CheckProc`。

不要实现 75% 进攻偏向。不要减自动射击自己的 CD。

- [ ] **Step 3: SQL**

```sql
-- 837 blizzlike: Whispered Truths (Whispering Eldritch Bow).
-- Auto-shot trims one hunter class cooldown currently ticking. Do NOT write spell_proc.

DELETE FROM `spell_script_names` WHERE `spell_id` = 316780;
INSERT INTO `spell_script_names` (`spell_id`, `ScriptName`) VALUES
(316780, 'spell_whispered_truths');
```

- [ ] **Step 4: 编译**

- [ ] **Step 5: 进游戏（猎人）**

开急速射击或奇美拉射击让转盘转，再自动射击。预期：`WHISPER_PROC spell=` 非 0；转盘跳约 2 秒。全部技能就绪时允许 `skipped=none_down`。

- [ ] **Step 6: 提交**

```
core/spells: implement Whispered Truths Ny'alotha weapon
```

---

### Task 5: 虚空之鞭

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`
- Create: `sql/updates/world/2026_08_29_07_world_corruption_lash_of_the_void.sql`
- Modify: `addons/HavenLab/HavenLab_Packs.lua`

**Interfaces:**
- Consumes: `CorruptionRankItemContext`、`CalcValue`、`SPELL_LASH_OF_THE_VOID_*`
- Produces: `int32 LashOfTheVoidDamage(Unit const*)`

热修后 317290 的 Trigger 已清 0。默认实现是施放 **317291**，不要把伤害打在 317290 自己身上。Task 0 若证明 317291 不存在，再停下来问，不要发明第三种。

```cpp
int32 LashOfTheVoidDamage(Unit const* owner)
{
    SpellInfo const* info = sSpellMgr->GetSpellInfo(SPELL_LASH_OF_THE_VOID);
    SpellEffectInfo const* effect = info ? info->GetEffect(EFFECT_1) : nullptr;
    if (!effect)
    {
        static bool logged = false;
        if (!logged)
        {
            logged = true;
            TC_LOG_ERROR("scripts", "LashOfTheVoid: scaling Dummy missing");
        }
        return 1;
    }
    uint32 itemId = 0;
    int32 itemLevel = -1;
    CorruptionRankItemContext(owner, SPELL_LASH_OF_THE_VOID, itemId, itemLevel);
    int32 value = effect->CalcValue(owner, nullptr, owner, nullptr, itemId, itemLevel);
    return value < 1 ? 1 : value;
}

class spell_lash_of_the_void : public AuraScript
{
    PrepareAuraScript(spell_lash_of_the_void);

    bool Validate(SpellInfo const* /*spellInfo*/) override
    {
        return ValidateSpellInfo({ SPELL_LASH_OF_THE_VOID_DAMAGE, SPELL_LASH_OF_THE_VOID_SLOW });
    }

    bool CheckProc(ProcEventInfo& eventInfo)
    {
        return (eventInfo.GetTypeMask() & PROC_FLAG_DONE_MELEE_AUTO_ATTACK) != 0;
    }

    void HandleProc(ProcEventInfo& eventInfo)
    {
        PreventDefaultAction();
        Unit* caster = GetTarget();
        Unit* target = eventInfo.GetActionTarget();
        if (!caster || !target)
            return;
        int32 damage = LashOfTheVoidDamage(caster);
        caster->CastCustomSpell(SPELL_LASH_OF_THE_VOID_DAMAGE, SPELLVALUE_BASE_POINT0, damage,
            caster, InfiniteStarsCastFlags());
        LabNotify(caster, "LASH_PROC", Trinity::StringFormat(
            "target=%s damage=%d", target->GetName().c_str(), damage));
    }

    void Register() override
    {
        DoCheckProc += AuraCheckProcFn(spell_lash_of_the_void::CheckProc);
        OnProc += AuraProcFn(spell_lash_of_the_void::HandleProc);
    }
};

class spell_lash_of_the_void_damage : public SpellScript
{
    PrepareSpellScript(spell_lash_of_the_void_damage);

    void HandleHit()
    {
        Unit* caster = GetCaster();
        if (!caster)
            return;
        int32 damage = LashOfTheVoidDamage(caster);
        SetHitDamage(damage);
    }

    void Register() override
    {
        OnHit += SpellHitFn(spell_lash_of_the_void_damage::HandleHit);
    }
};
```

`317291` 目标是施法者面前锥形，所以 `CastCustomSpell` 的目标是 **caster**，不要对平砍目标单体施放（会丢掉锥）。减速由 317291 → 319241 自己挂，不要 `CastSpell(319241)`。不要施放 317292。Task 0 若 317290 的 ProcFlags 已经是 4：`CheckProc` 可删，只留目标非空。

若 Task 0 显示缩放 Dummy 在 EFFECT_0 而不是 EFFECT_1：只改 `GetEffect` 下标。

- [ ] **Step 1: pack**（`key = "lash"`，`labType = "LASH_PROC"`，ids 317290 / 317291 / 319241）

- [ ] **Step 2: 写脚本并 `RegisterAuraScript` / `RegisterSpellScript`**

- [ ] **Step 3: SQL**

```sql
-- 837 blizzlike: Lash of the Void (Unguent Caress).
-- 35662 ProcTypeMask=4 (melee auto only). Damage = 317290 Dummy CalcValue
-- into 317291 (9yd / 60deg cone). Slow is 319241 via DBC trigger.
-- Do NOT write spell_proc. Do NOT cast 317292.

DELETE FROM `spell_script_names` WHERE `spell_id` IN (317290, 317291);
INSERT INTO `spell_script_names` (`spell_id`, `ScriptName`) VALUES
(317290, 'spell_lash_of_the_void'),
(317291, 'spell_lash_of_the_void_damage');
```

- [ ] **Step 4: 编译**

- [ ] **Step 5: 进游戏**

平砍出暗影锥伤和 6 秒减速。猛击不应出。身后桩不应吃。第二只桩站在锥里应吃满额（不均摊）。

- [ ] **Step 6: 提交**

```
core/spells: implement Lash of the Void Ny'alotha weapon
```

---

### Task 6: 灼热烈焰

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`
- Create: `sql/updates/world/2026_08_29_08_world_corruption_searing_flames.sql`
- Modify: `addons/HavenLab/HavenLab_Packs.lua`

**Interfaces:**
- Consumes: `SPELL_SEARING_FLAMES_*`、Task 0 的 316704 TargetA/B
- Produces: `int32 SearingBreathHealthPct(Unit const*)`、`void TrySearingBreath(Unit*, Unit*)`

- [ ] **Step 1: pack**（`key = "searing"`，期望 `SEARING_BREATH`，ids 318293/316698/316703/316704）

- [ ] **Step 2: 脚本**

```cpp
int32 SearingBreathHealthPct(Unit const* owner)
{
    uint32 const ranks[] = { SPELL_SEARING_FLAMES_ITEM };
    int32 pct = SumCorruptionRankDummy(owner, ranks, 1, EFFECT_0, true,
        SEARING_BREATH_PCT_FALLBACK, "SearingFlames");
    return pct < 1 ? SEARING_BREATH_PCT_FALLBACK : pct;
}

uint32 SearingFlamesMaxStacks()
{
    if (SpellInfo const* buff = sSpellMgr->GetSpellInfo(SPELL_SEARING_FLAMES_BUFF))
        if (buff->StackAmount >= 1)
            return buff->StackAmount;
    static bool logged = false;
    if (!logged)
    {
        logged = true;
        TC_LOG_ERROR("scripts", "SearingFlames: StackAmount missing, using %u",
            SEARING_FLAMES_MAX_STACKS_FALLBACK);
    }
    return SEARING_FLAMES_MAX_STACKS_FALLBACK;
}

void TrySearingBreath(Unit* caster, Unit* facingTarget)
{
    if (!caster)
        return;
    Aura* buff = caster->GetAura(SPELL_SEARING_FLAMES_BUFF);
    uint32 maxStacks = SearingFlamesMaxStacks();
    if (!buff)
    {
        caster->CastSpell(caster, SPELL_SEARING_FLAMES_BUFF, InfiniteStarsCastFlags());
        buff = caster->GetAura(SPELL_SEARING_FLAMES_BUFF);
        if (buff)
            buff->SetStackAmount(1);
        LabNotify(caster, "SEARING_STACK", "stacks=1");
        return;
    }

    uint32 stacks = uint32(buff->GetStackAmount()) + 1;
    if (stacks < maxStacks)
    {
        buff->SetStackAmount(int32(stacks));
        LabNotify(caster, "SEARING_STACK", Trinity::StringFormat("stacks=%u", stacks));
        return;
    }

    if (facingTarget && facingTarget != caster)
        caster->SetInFront(facingTarget);
    bool ok = caster->CastSpell(caster, SPELL_SEARING_FLAMES_BREATH, InfiniteStarsCastFlags());
    buff->Remove();
    LabNotify(caster, "SEARING_BREATH", Trinity::StringFormat(
        "pct=%d ok=%u", SearingBreathHealthPct(caster), ok ? 1u : 0u));
}

class spell_searing_flames_proc : public AuraScript
{
    PrepareAuraScript(spell_searing_flames_proc);

    bool CheckProc(ProcEventInfo& eventInfo)
    {
        // White hits are excluded by spell_proc mask 69904. Do NOT also require
        // StartRecoveryTime: no-GCD damaging yellows still stack. Do NOT require
        // GetDamage()>0: one multi-hit cast is already gated by the 100ms ICD.
        Spell const* procSpell = eventInfo.GetProcSpell();
        return procSpell && procSpell->GetSpellInfo();
    }

    void HandleProc(ProcEventInfo& eventInfo)
    {
        PreventDefaultAction();
        TrySearingBreath(GetTarget(), eventInfo.GetActionTarget());
    }

    void HandleRemove(AuraEffect const* /*aurEff*/, AuraEffectHandleModes /*mode*/)
    {
        if (Unit* owner = GetTarget())
            owner->RemoveAurasDueToSpell(SPELL_SEARING_FLAMES_BUFF);
    }

    void Register() override
    {
        DoCheckProc += AuraCheckProcFn(spell_searing_flames_proc::CheckProc);
        OnProc += AuraProcFn(spell_searing_flames_proc::HandleProc);
        AfterEffectRemove += AuraEffectRemoveFn(spell_searing_flames_proc::HandleRemove,
            EFFECT_0, SPELL_AURA_ANY, AURA_EFFECT_HANDLE_REAL);
    }
};

class spell_searing_breath : public SpellScript
{
    PrepareSpellScript(spell_searing_breath);

    void HandleHit()
    {
        Unit* caster = GetCaster();
        if (!caster)
            return;
        int64 damage = int64(caster->GetMaxHealth()) * SearingBreathHealthPct(caster) / 100;
        if (damage < 1)
            damage = 1;
        SetHitDamage(int32(damage));
    }

    void Register() override
    {
        OnHit += SpellHitFn(spell_searing_breath::HandleHit);
    }
};
```

316704 已是 `TARGET_UNIT_CONE_ENEMY_104`、半径 12 码、锥角 60 度。**不要写 `FilterTargets`。** 核心 `SelectImplicitConeTargets` 负责视线（2020-02-04 热修）和碰撞体（2020-02-05 大体型）。

- [ ] **Step 3: SQL**

```sql
-- 837 blizzlike: Searing Flames (Faralos).
-- 35662 SpellAuraOptions 316698: ProcTypeMask_0=0, ProcTypeMask_1=0x4 (cast successful).
-- This core only loads mask0 into SpellInfo::ProcFlags, so the aura never procs
-- without a spell_proc row. Restore mask0=69904 (yellow melee/ranged + hostile
-- none/magic; no autos, no DoT ticks), SpellTypeMask=DAMAGE, ICD 100ms.
-- Do NOT copy SimC PF_ALL_DAMAGE / CAST_HEAL / aoe split.

DELETE FROM `spell_script_names` WHERE `spell_id` IN (316698, 316704);
INSERT INTO `spell_script_names` (`spell_id`, `ScriptName`) VALUES
(316698, 'spell_searing_flames_proc'),
(316704, 'spell_searing_breath');

DELETE FROM `spell_proc` WHERE `SpellId` = 316698;
INSERT INTO `spell_proc` (
    `SpellId`,
    `SchoolMask`,
    `SpellFamilyName`,
    `SpellFamilyMask0`,
    `SpellFamilyMask1`,
    `SpellFamilyMask2`,
    `SpellFamilyMask3`,
    `ProcFlags`,
    `SpellTypeMask`,
    `SpellPhaseMask`,
    `HitMask`,
    `AttributesMask`,
    `DisableEffectsMask`,
    `ProcsPerMinute`,
    `Chance`,
    `Cooldown`,
    `Charges`
) VALUES (
    316698,
    0,
    0,
    0,
    0,
    0,
    0,
    69904,
    1,
    2,
    0,
    0,
    0,
    0,
    101,
    100,
    0
);
```

列顺序必须和 `2026_08_26_02_world_corruption_echoing_void.sql` 一致。`69904` = `0x11110`。`SpellPhaseMask=2`（命中）是本核 `REQ_SPELL_PHASE_PROC_FLAG_MASK` 对这组 flag 的硬要求；未命中是否叠层进游戏确认，不要为了「施法完成」去改核心。

若 proc 在 318293：把 316698 换成 318293。

- [ ] **Step 4: 编译 `worldserver`**

- [ ] **Step 5: 进游戏**

伤害技能叠层，第 30 次立刻喷（不是第 31 次）。身后的桩不应吃。治疗技能不应叠。平砍不应叠。`.lab clear` 后 316703 必须跟着掉。

- [ ] **Step 6: 提交**

```
core/spells: implement Searing Flames Ny'alotha weapon
```

---

### Task 7: 黑曜石皮肤

**Files:**
- Modify: `src/server/scripts/Spells/spell_corruption.cpp`
- Create: `sql/updates/world/2026_08_29_09_world_corruption_obsidian_skin.sql`
- Modify: `addons/HavenLab/HavenLab_Packs.lua`

**Interfaces:**
- Consumes: `SPELL_OBSIDIAN_*`、末路恶果「仅战斗跳」的形状（`IsInCombat`）
- Produces: `int32 ObsidianDestructionArmorPct(Unit const*)`、`void TickObsidianDestruction(Unit*)`

- [ ] **Step 1: pack**（`key = "obsidian"`，`OBSIDIAN_TICK` / `OBSIDIAN_BOOM`，ids 316651/317420/316661）

- [ ] **Step 2: 脚本**

316651 护甲效果：**不要**写 AuraScript，除非转储 BP=0。

```cpp
int32 ObsidianDestructionArmorPct(Unit const* owner)
{
    if (SpellInfo const* driver = sSpellMgr->GetSpellInfo(SPELL_OBSIDIAN_SKIN))
        if (SpellEffectInfo const* effect = driver->GetEffect(EFFECT_2))
        {
            int32 pct = effect->BasePoints;
            if (pct >= 1)
                return pct;
        }
    static bool logged = false;
    if (!logged)
    {
        logged = true;
        TC_LOG_ERROR("scripts", "ObsidianSkin: Dummy3 missing, using %d",
            OBSIDIAN_ARMOR_PCT_FALLBACK);
    }
    return OBSIDIAN_ARMOR_PCT_FALLBACK;
}

uint32 ObsidianMaxStacks()
{
    if (SpellInfo const* tick = sSpellMgr->GetSpellInfo(SPELL_OBSIDIAN_DESTRUCTION))
        if (tick->StackAmount >= 1)
            return tick->StackAmount;
    return 30;
}

void TickObsidianDestruction(Unit* owner)
{
    if (!owner || !owner->IsAlive() || !owner->IsInCombat())
        return;

    Aura* buff = owner->GetAura(SPELL_OBSIDIAN_DESTRUCTION);
    if (!buff)
    {
        owner->CastSpell(owner, SPELL_OBSIDIAN_DESTRUCTION, InfiniteStarsCastFlags());
        buff = owner->GetAura(SPELL_OBSIDIAN_DESTRUCTION);
        if (buff)
            buff->SetStackAmount(1);
        LabNotify(owner, "OBSIDIAN_TICK", "stacks=1");
        return;
    }

    uint32 maxStacks = ObsidianMaxStacks();
    uint32 stacks = uint32(buff->GetStackAmount());
    if (stacks < maxStacks)
    {
        buff->SetStackAmount(int32(stacks + 1));
        LabNotify(owner, "OBSIDIAN_TICK", Trinity::StringFormat("stacks=%u", stacks + 1));
        return;
    }

    // Already at cap (including pre-stacked out of combat): explode this tick, then back to 1.
    int32 pct = ObsidianDestructionArmorPct(owner);
    int64 damage = int64(owner->GetArmor()) * pct / 100;
    if (damage < 1)
        damage = 1;
    owner->CastCustomSpell(SPELL_OBSIDIAN_DESTRUCTION_DAMAGE, SPELLVALUE_BASE_POINT0,
        int32(damage), owner, InfiniteStarsCastFlags());
    buff->SetStackAmount(1);
    LabNotify(owner, "OBSIDIAN_BOOM", Trinity::StringFormat(
        "armor=%u pct=%d damage=%d", owner->GetArmor(), pct, int32(damage)));
}

class spell_obsidian_destruction : public AuraScript
{
    PrepareAuraScript(spell_obsidian_destruction);

    bool Validate(SpellInfo const* /*spellInfo*/) override
    {
        return ValidateSpellInfo({ SPELL_OBSIDIAN_DESTRUCTION_DAMAGE, SPELL_OBSIDIAN_SKIN });
    }

    void HandlePeriodic(AuraEffect const* /*aurEff*/)
    {
        PreventDefaultAction();
        TickObsidianDestruction(GetTarget());
    }

    void Register() override
    {
        OnEffectPeriodic += AuraEffectPeriodicFn(spell_obsidian_destruction::HandlePeriodic,
            EFFECT_0, SPELL_AURA_PERIODIC_DUMMY);
    }
};

class spell_obsidian_destruction_damage : public SpellScript
{
    PrepareSpellScript(spell_obsidian_destruction_damage);

    uint32 _targets = 1;

    void CountTargets(std::list<WorldObject*>& targets)
    {
        _targets = targets.empty() ? 1u : uint32(targets.size());
    }

    void HandleHit()
    {
        Unit* caster = GetCaster();
        if (!caster)
            return;
        int32 pct = ObsidianDestructionArmorPct(caster);
        int64 base = int64(caster->GetArmor()) * pct / 100;
        if (base < 1)
            base = 1;
        uint32 n = std::min(_targets, OBSIDIAN_SPLIT_CAP);
        float totalMul = 1.0f + OBSIDIAN_SPLIT_PER_TARGET * float(n - 1);
        int64 damage = int64(float(base) * totalMul / float(_targets));
        if (damage < 1)
            damage = 1;
        SetHitDamage(int32(damage));
    }

    void Register() override
    {
        OnObjectAreaTargetSelect += SpellObjectAreaTargetSelectFn(
            spell_obsidian_destruction_damage::CountTargets, EFFECT_0, TARGET_UNIT_SRC_AREA_ENEMY);
        OnHit += SpellHitFn(spell_obsidian_destruction_damage::HandleHit);
    }
};
```

`CountTargets` 的目标枚举必须改成转储的 TargetA。不要把这套分摊抄到灼热吐息。

要点：

- `PreventDefault` 必须有：317420 若带 TriggerSpell，核心可能自己放一个空技能。
- **脱战不 `Remove` 317420**。只是 `HandlePeriodic` 里 `!IsInCombat()` 直接 return。层保留。
- 进战斗时已是 30 层：本跳就炸，不要再 +1。
- `.lab test obsidian` 挂的是 316651。若 316651 不会 LINKED 317420：在 316651 上写一个最小 `AfterApply`，只 `CastSpell(317420)`，脱时摘 317420。先看转储再决定要不要这 8 行。
- Dummy 下标：上面写 `EFFECT_2`（第三效果）。转储若 Dummy 700 在别的 index，只改 `GetEffect` 参数。
- 不要写双持加倍。+5% 护甲零脚本。

- [ ] **Step 3: SQL**

```sql
-- 837 blizzlike: Obsidian Skin (Sk'shuul Vaz). 317420 ticks in combat only;
-- stacks persist out of combat. 316661 damage = current armor * Dummy3.
-- 2020-01-28 split (SimC 15%/extra, cap 6) is script-side; DBC has no field.
-- Do NOT reset stacks on combat start. Do NOT write spell_proc. Do NOT double on dual 2H.

DELETE FROM `spell_script_names` WHERE `spell_id` IN (317420, 316661);
INSERT INTO `spell_script_names` (`spell_id`, `ScriptName`) VALUES
(317420, 'spell_obsidian_destruction'),
(316661, 'spell_obsidian_destruction_damage');
```

- [ ] **Step 4: 编译**

- [ ] **Step 5: 进游戏**

进战斗看层涨。到 30 后爆炸，层回 1。脱战层还在。20 码外的桩不受伤。两个桩时每人应低于单目标满额（分摊）。身后/视线外不应吃。

- [ ] **Step 6: 提交**

```
core/spells: implement Obsidian Skin Ny'alotha weapon
```

---

### Task 8: 回填文档

**Files:**
- Modify: `doc/837满级修复/腐蚀特效清单.md`（六条 Haven 列）
- Modify: `doc/837满级修复/设计-腐蚀-Blizzlike.md`（热修表加 P6 行）
- Modify: `doc/837满级修复/README.md`（近期任务）
- Modify: `doc/837满级修复/设计-P6-尼奥罗萨武器.md` 第 10 节（转储结论）

**Interfaces:**
- Consumes: 各条进游戏结果。第 1 层已于 2026-08-29 进游戏验收通过。未进游戏的条目必须写「代码完成，待进游戏验收」，禁止写「通过」。

- [ ] **Step 1: 清单 C 节 Haven 列按实际状态改**

- [ ] **Step 2: 热修表抄设计文档第 7 节里真正用到的常量**（灼热登记 100ms ICD 与 mask0=69904；黑曜石登记 700 与 15%/6 分摊；不要再写 90 度锥）

- [ ] **Step 3: README「正在做」改成 P6 当前条**

- [ ] **Step 4: 提交**

```
docs: record P6 Ny'alotha weapon dump and lab status
```

---

## Self-Review

**1. Spec coverage**

| 设计文档节 | 对应任务 |
|------------|----------|
| 第 1 节第三套 / 不改 UpdateCorruption | 全局约束 + 文件总表 |
| 第 3 节 bonus 腐蚀值 | 不写进脚本，只写文档 |
| 4.1 吞噬 | Task 2 |
| 4.2 灼热 | Task 6 |
| 4.3 低语 | Task 4 |
| 4.4 法器 | Task 3 |
| 4.5 鞭 | Task 5 |
| 4.6 黑曜石（317420 叠层 / 316661 爆炸、层保留、分摊、不双持加倍） | Task 7 |
| 第 6 节路径 A | 全部任务同一文件 |
| 第 7 节 fallback | Task 1 constexpr + 各求值函数 |
| 第 8 节 HavenLab | 每条效果的 Step 1 pack |
| 第 9 节不做 | 无任务去实现双持双手、75% 进攻、Glimpse 混用 |
| 第 10 节转储 | Task 0 |

**2. Placeholder scan:** Task 5 默认施放 317291，不再留「情况 A / B」。EffectIndex 仍以 Task 0 转储为准。没有「类似某任务」的省略。

**3. Type consistency:** `LabNotify` 签名始终是 `(Unit*, char const*, std::string const&)`。`InfiniteStarsCastFlags` 无参数。`SumCorruptionRankDummy` 签名未改。`FLASH_ROLL` / `DEVOUR_PROC` / `WHISPER_PROC` / `LASH_PROC` / `SEARING_STACK` / `SEARING_BREATH` / `OBSIDIAN_TICK` / `OBSIDIAN_BOOM` 与 pack `labType` 一一对应。
